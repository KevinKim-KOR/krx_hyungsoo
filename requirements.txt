fastapi>=0.110
uvicorn>=0.27
pydantic>=2.5
pytest>=8.0
httpx>=0.27
python-dotenv>=1.0
pykrx>=1.0.51
# pykrx 1.0.51 가 pkg_resources 를 import 하지만 자체 requirements 에 명시 안 함.
# Python 3.12+ venv 는 setuptools 가 기본 포함이 아니다.
# setuptools 81 부터 pkg_resources 가 분리되어 import 실패하므로 <81 제약 필요.
# (재검토 트리거: pykrx 가 pkg_resources 의존성을 제거한 새 버전 출시)
setuptools>=68.0,<81
# FinanceDataReader — B 방향 PC 작업 1~2단계 ETF universe / 가격 시계열 1순위 소스.
# pykrx ETF universe 자동 조회 경로 폐기 후 채택 (가능성 확인 STEP PASS — 2026-05-15).
finance-datareader>=0.9.202
# torch — ML 축1 (후보 ETF 상대상승 참고점수 v0) STEP (2026-06-20).
# PC 4070 SUPER GPU 학습용. 사용자 결정 — 지시문 §6.1 의 "신규 ML 라이브러리
# 추가 금지" 의 예외 1회 허용.
# 기존 baseline 모듈 자체는 단일 선형회귀를 유지한다. POC4 연구 경로에서는
# 사전 정의된 Gate 에 따라 단순 모멘텀과 RandomForest 비교를 허용한다. 자동
# 튜닝과 XGBoost·LightGBM 은 별도 승인 전까지 금지한다(설계자 POC4-00 2026-09-23).
# CUDA 12.4 wheel:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
torch>=2.6.0
# scikit-learn — Market Flow ML Dataset + Baseline v1 Closeout (2026-07-05).
# 지시문 §6 "scikit-learn 하나만 프로젝트 의존성으로 추가" 명시 허용 +
# "실제 설치된 정확한 버전 고정" 재현성 요건 (FIX r1 정정).
# StandardScaler + Ridge(alpha=1.0) 를 쓴다. RandomForest 도 scikit-learn 내장이라
# 신규 의존성 없이 POC4 연구 경로 비교에 쓴다.
# 기존 baseline 모듈 자체는 단일 선형회귀를 유지한다. POC4 연구 경로에서는
# 사전 정의된 Gate 에 따라 단순 모멘텀과 RandomForest 비교를 허용한다. 자동
# 튜닝과 XGBoost·LightGBM 은 별도 승인 전까지 금지한다(설계자 POC4-00 2026-09-23).
scikit-learn==1.9.0
# numpy — Market Flow ML v2 Data Validity + Model Comparison (2026-07-05).
# 지시문 Q1 (b) 확정: quantile method="linear" / target 분포 percentile / mean /
# stddev / min / max 등을 numpy 로 직접 계산. transitive dependency 회피 위해
# 명시 선언 + 실제 설치 버전 정확 고정. 버전 범위 표기 금지.
numpy==2.4.6
