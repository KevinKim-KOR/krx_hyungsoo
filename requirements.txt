fastapi>=0.110
uvicorn>=0.27
pydantic>=2.5
pytest>=8.0
httpx>=0.27
python-dotenv>=1.0
pykrx>=1.0.51
# pykrx 1.0.51 가 pkg_resources 를 import 하지만 자체 requirements 에 명시 안 함.
# Python 3.12+ venv 는 setuptools 가 기본 포함이 아니다.
# setuptools 81 부터 pkg_resources 가 분리되어 import 실패하므로 <81 제약 필요.
# (재검토 트리거: pykrx 가 pkg_resources 의존성을 제거한 새 버전 출시)
setuptools>=68.0,<81
