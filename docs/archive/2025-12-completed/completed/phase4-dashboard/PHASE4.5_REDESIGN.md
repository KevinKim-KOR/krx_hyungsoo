# Phase 4.5 재설계: FastAPI + React 전환

## 🎯 **목표**

Streamlit → FastAPI + React 전환으로 프로덕션 환경 구축

---

## 📊 **현재 문제점**

### **1. 중복된 페이지**
```
기존 9개 페이지:
- app.py (메인)
- home.py (홈)
- performance.py (성과) ← 중복
- portfolio.py (포트폴리오) ← 중복
- signals.py (신호) ← 중복
- regime.py (레짐) ← 불명확
- alerts.py (알림) ← 중복
- backtest.py (백테스트) ← 신규
- stop_loss.py (손절) ← 신규
```

### **2. 누락된 기능**
```
❌ 자산 입력/관리
❌ 매수/매도 기록
❌ 백테스트 실행 UI
❌ 파라미터 비교
❌ 최적 vs 현재 조건
```

### **3. Streamlit 한계**
```
❌ 프로덕션 환경 부적합
❌ 커스터마이징 제한
❌ 성능 제한
❌ 로컬/클라우드 분리 어려움
```

---

## 🎯 **새로운 UI 구조 (6개 페이지)**

### **1. 🏠 홈 (대시보드)**
```
목적: 전체 현황 한눈에 파악

섹션:
┌─────────────────────────────────────┐
│ 자산 현황                           │
│ - 총 자산: ₩100,000,000            │
│ - 현금: ₩20,000,000                │
│ - 투자금: ₩80,000,000              │
│ - 수익률: +15.5%                   │
├─────────────────────────────────────┤
│ 현재 주식 현황                      │
│ [종목 리스트 테이블]                │
│ - 종목명, 수량, 평단가, 현재가      │
│ - 수익률, 수익금                    │
├─────────────────────────────────────┤
│ 투자 요약                           │
│ - 오늘: +₩500,000 (+0.5%)          │
│ - 이번 주: +₩2,000,000 (+2.0%)     │
│ - 이번 달: +₩5,000,000 (+5.0%)     │
├─────────────────────────────────────┤
│ 빠른 액션                           │
│ [매수] [매도] [백테스트] [손절체크] │
└─────────────────────────────────────┘
```

### **2. 💼 자산 관리**
```
목적: 자산 입력 및 거래 기록 관리

기능:
1. 자산 현황
   - 총 자산 차트 (시계열)
   - 자산 구성 (현금 vs 주식)
   - 섹터별 분포

2. 거래 기록
   - 매수 기록 테이블
   - 매도 기록 테이블
   - 추가 매수 기록

3. 자산 입력 폼
   ┌─────────────────────────────┐
   │ 종목 추가                   │
   │ 종목명: [_______]           │
   │ 종목코드: [_______]         │
   │ 매수가: [_______]           │
   │ 수량: [_______]             │
   │ 매수일: [_______]           │
   │ [추가] [취소]               │
   └─────────────────────────────┘

4. 거래 히스토리
   - 전체 거래 내역
   - 필터 (기간, 종목, 유형)
   - CSV 다운로드
```

### **3. 📊 백테스트**
```
목적: 백테스트 실행 및 결과 분석

기능:
1. 백테스트 실행 (로컬만)
   ┌─────────────────────────────┐
   │ 백테스트 파라미터           │
   │ 시작일: [_______]           │
   │ 종료일: [_______]           │
   │ 초기자금: [_______]         │
   │ Top N: [_______]            │
   │ 리밸런싱: [일별▼]           │
   │ [실행] [저장]               │
   └─────────────────────────────┘

2. 최근 백테스트 결과
   - CAGR, Sharpe, MDD
   - 자산 곡선
   - 낙폭 차트

3. 파라미터 히스토리
   - 과거 백테스트 목록
   - 파라미터별 성과 비교

4. 최적 조건 vs 현재 조건
   ┌─────────────────────────────┐
   │ 비교 분석                   │
   │ 항목      최적    현재      │
   │ Top N     10      15        │
   │ CAGR      35%     27%       │
   │ Sharpe    1.8     1.5       │
   │ MDD       -18%    -20%      │
   │ [최적 조건 적용]            │
   └─────────────────────────────┘
```

### **4. 🎯 손절 전략**
```
목적: 손절 전략 비교 및 최적화

기능:
1. 4가지 전략 비교
   - 고정 손절
   - 레짐별 손절
   - 동적 손절
   - 하이브리드 손절

2. 최적 전략 vs 현재 전략
   ┌─────────────────────────────┐
   │ 전략 비교                   │
   │ 항목        최적    현재    │
   │ 전략        하이브  고정    │
   │ 개선 효과   +27.8%  +27.0%  │
   │ 손절 대상   6개     5개     │
   │ [최적 전략 적용]            │
   └─────────────────────────────┘

3. 손절 대상 종목
   - 실시간 손절 대상
   - 손절 근접 종목
   - 알림 설정

4. 전략 파라미터 조정 (로컬만)
   - 손절 기준 변경
   - 백테스트로 검증
```

### **5. 📈 신호 & 히스토리**
```
목적: 매매 신호 및 파라미터 히스토리 관리

기능:
1. 매매 신호 히스토리
   - 매수 신호 목록
   - 매도 신호 목록
   - 신호 강도 차트

2. 백테스트 파라미터 히스토리
   - 파라미터 변경 이력
   - 성과 추이
   - 최적 파라미터 추천

3. 룩백 히스토리
   - 룩백 기간별 성과
   - 파라미터 조합 분석

4. 알림 히스토리
   - 텔레그램 알림 기록
   - 손절 알림 기록
   - 일일/주간 리포트 기록
```

### **6. 🌡️ 시장 분석**
```
목적: 시장 상황 분석 (명확한 기준)

기능:
1. 시장 레짐
   ┌─────────────────────────────┐
   │ 현재 레짐: 🟢 상승장        │
   │                             │
   │ 기준:                       │
   │ - MA50 > MA200: ✅          │
   │ - 추세 강도: 85%            │
   │ - 변동성: 낮음              │
   │                             │
   │ 레짐 히스토리:              │
   │ [차트]                      │
   └─────────────────────────────┘

2. 변동성 분석
   - ATR (평균 진폭)
   - 볼린저 밴드
   - 변동성 추이

3. 섹터 분석
   - 섹터별 수익률
   - 강세/약세 섹터
   - 섹터 로테이션

4. 시장 지표
   - KOSPI/KOSDAQ 추이
   - 외국인/기관 매매
   - 시장 심리 지표
```

---

## 🏗️ **기술 스택**

### **Frontend (React)**
```
- React 18
- TypeScript
- TailwindCSS (스타일링)
- Recharts / Chart.js (차트)
- React Router (라우팅)
- Axios (API 통신)
- React Query (데이터 페칭)
```

### **Backend (FastAPI)**
```
- FastAPI 0.100+
- Python 3.10+
- SQLAlchemy (ORM)
- Pydantic (데이터 검증)
- Alembic (마이그레이션)
- python-jose (JWT)
```

### **Database**
```
- SQLite (로컬 개발)
- PostgreSQL (Oracle Cloud)
```

### **Deployment**
```
- Docker
- Docker Compose
- Nginx (리버스 프록시)
- Let's Encrypt (SSL)
```

---

## 📁 **프로젝트 구조**

```
krx_alertor_modular/
├── backend/                    # FastAPI 백엔드
│   ├── app/
│   │   ├── api/               # API 엔드포인트
│   │   │   ├── v1/
│   │   │   │   ├── dashboard.py
│   │   │   │   ├── assets.py
│   │   │   │   ├── backtest.py
│   │   │   │   ├── stop_loss.py
│   │   │   │   ├── signals.py
│   │   │   │   └── market.py
│   │   │   └── deps.py
│   │   ├── core/              # 설정, 보안
│   │   │   ├── config.py
│   │   │   ├── security.py
│   │   │   └── database.py
│   │   ├── models/            # DB 모델
│   │   │   ├── asset.py
│   │   │   ├── trade.py
│   │   │   ├── backtest.py
│   │   │   └── signal.py
│   │   ├── schemas/           # Pydantic 스키마
│   │   │   ├── asset.py
│   │   │   ├── trade.py
│   │   │   └── backtest.py
│   │   ├── services/          # 비즈니스 로직
│   │   │   ├── asset_service.py
│   │   │   ├── backtest_service.py
│   │   │   └── stop_loss_service.py
│   │   └── main.py
│   ├── tests/
│   ├── alembic/
│   ├── requirements.txt
│   └── Dockerfile
│
├── frontend/                   # React 프론트엔드
│   ├── public/
│   ├── src/
│   │   ├── components/        # 공통 컴포넌트
│   │   │   ├── Layout/
│   │   │   ├── Charts/
│   │   │   ├── Tables/
│   │   │   └── Forms/
│   │   ├── pages/             # 6개 페이지
│   │   │   ├── Dashboard.tsx
│   │   │   ├── Assets.tsx
│   │   │   ├── Backtest.tsx
│   │   │   ├── StopLoss.tsx
│   │   │   ├── Signals.tsx
│   │   │   └── Market.tsx
│   │   ├── services/          # API 서비스
│   │   │   └── api.ts
│   │   ├── hooks/             # Custom Hooks
│   │   ├── utils/             # 유틸리티
│   │   ├── types/             # TypeScript 타입
│   │   ├── App.tsx
│   │   └── index.tsx
│   ├── package.json
│   ├── tsconfig.json
│   ├── tailwind.config.js
│   └── Dockerfile
│
├── docker-compose.yml
├── nginx.conf
└── README.md
```

---

## 🚀 **구현 일정 (10일)**

### **Day 1: 프로젝트 초기화**
```
✅ FastAPI 프로젝트 생성
✅ React 프로젝트 생성
✅ Docker 설정
✅ DB 스키마 설계
```

### **Day 2: 백엔드 API (1/2)**
```
✅ 대시보드 API
✅ 자산 관리 API
✅ 거래 기록 API
```

### **Day 3: 백엔드 API (2/2)**
```
✅ 백테스트 API
✅ 손절 전략 API
✅ 신호 히스토리 API
✅ 시장 분석 API
```

### **Day 4: 프론트엔드 (홈)**
```
✅ 레이아웃 구성
✅ 네비게이션
✅ 홈 페이지 (대시보드)
```

### **Day 5: 프론트엔드 (자산)**
```
✅ 자산 관리 페이지
✅ 자산 입력 폼
✅ 거래 기록 테이블
```

### **Day 6: 프론트엔드 (백테스트)**
```
✅ 백테스트 페이지
✅ 실행 UI
✅ 결과 차트
✅ 파라미터 비교
```

### **Day 7: 프론트엔드 (손절 + 신호)**
```
✅ 손절 전략 페이지
✅ 신호 & 히스토리 페이지
```

### **Day 8: 프론트엔드 (시장 분석)**
```
✅ 시장 분석 페이지
✅ 레짐 분석 (명확한 기준)
✅ 변동성 분석
```

### **Day 9: 통합 테스트**
```
✅ 로컬 테스트
✅ API 통합 테스트
✅ UI/UX 개선
```

### **Day 10: Oracle Cloud 배포**
```
✅ Docker 빌드
✅ Oracle Cloud 배포
✅ Nginx + SSL 설정
✅ 최종 테스트
```

---

## 💡 **로컬 vs 클라우드 기능 분리**

### **로컬 전용 기능**
```
- 백테스트 실행 (CPU 집약적)
- 파라미터 조정
- 머신러닝 학습
- 대용량 데이터 처리
```

### **클라우드 기능**
```
- 대시보드 조회
- 자산 관리
- 거래 기록
- 신호 히스토리
- 시장 분석
```

### **구현 방법**
```python
# backend/app/core/config.py
class Settings:
    IS_LOCAL = os.getenv("IS_LOCAL", "false").lower() == "true"
    
# backend/app/api/v1/backtest.py
@router.post("/run")
async def run_backtest(params: BacktestParams):
    if not settings.IS_LOCAL:
        raise HTTPException(
            status_code=403,
            detail="백테스트는 로컬에서만 실행 가능합니다"
        )
    # 백테스트 실행
```

---

## 🎯 **다음 액션**

### **즉시 시작**
```
1. 기존 Streamlit 페이지 백업
2. FastAPI 프로젝트 생성
3. React 프로젝트 생성
4. DB 스키마 설계
5. API 엔드포인트 설계
```

### **확인 필요**
```
❓ FastAPI + React 전환 진행?
❓ 10일 일정 적절한가?
❓ 추가 요구사항 있는가?
```

---

**Phase 4.5 재설계 완료!** ✅  
**FastAPI + React 전환 준비 완료!** 🚀  
**시작하시겠습니까?** 😊
