# KRX Alertor 개발 계획 (2025-12-11 기준)

---

## ✅ 긴급 (완료됨 - 2025-12-11)

| # | 작업 | 설명 | 상태 |
|---|------|------|------|
| 1 | **Live 파라미터 구조 구현** | `optimal_params.json`에 `live` 블록 분리 | ✅ 완료 |
| 2 | **"실전 파라미터로 적용" 버튼** | 튜닝 UI에서 Live 승격 기능 | ✅ 완료 |
| 3 | **일일 추천 알림 신규 개발** | Live 파라미터 기반 매수/매도 신호 | ✅ 완료 |
| 4 | **RSI 프로파일 YAML** | RSI 스케일링 규칙 외부화 (전략 설계값) | ✅ 완료 |
| 5 | **Live 파라미터 수동 설정 UI** | 튜닝 없이 직접 파라미터 입력 | ✅ 완료 |
| 6 | **RSI 프로파일 3단계 선택 UI** | conservative/neutral/aggressive 선택 | ✅ 완료 |

### 구현 파일
- `app/services/optimal_params_service.py` - live/live_history/research 구조
- `api_backtest.py` - promote-to-live, set-live, live, rollback-live, rsi-profiles API
- `web/dashboard/src/pages/Strategy.tsx` - Live 수동 설정 UI + RSI 프로파일 선택
- `core/strategy/live_signal_generator.py` - Live 기반 신호 생성 + RSI 프로파일 로드
- `scripts/nas/daily_recommendation_alert.py` - 일일 추천 알림
- `config/rsi_profiles.yaml` - RSI 스케일링 프로파일 (3단계)

### RSI 프로파일 3단계
| 프로파일 | 과매수 컷 | Bull 부스트 | 설명 |
|----------|-----------|-------------|------|
| **conservative** | RSI 70+ | 1.1x | 안전 우선, 과매수 회피 강화 |
| **neutral** | RSI 65+ | 1.2x | 균형잡힌 기본 설정 |
| **aggressive** | RSI 70+ | 1.3x | 수익 추구, 과매도 부스트 강화 |

```yaml
# config/rsi_profiles.yaml
profiles:
  conservative:
    overbought: [85→0, 80→0.3, 70→0.6]
    oversold: bull 1.1x / neutral,bear 1.0x
  neutral:
    overbought: [85→0, 75→0.3, 65→0.7]
    oversold: bull 1.2x / neutral,bear 1.0x
  aggressive:
    overbought: [90→0, 80→0.3, 70→0.8]
    oversold: bull 1.3x / neutral,bear 1.0x
```

---

## ⭐ 중요 (2주 내 완료)

| # | 작업 | 설명 | 예상 시간 |
|---|------|------|-----------|
| 7 | **기존 알림에 Live 파라미터 연동** | 장시작/일일/주간 리포트 수정 | 3시간 |
| 8 | **손절 합성 로직** | ETF: `min(전략 stop_loss, 하이브리드)` | 2시간 |
| 9 | **lookback 파라미터 추가** | `optimal_params.json`에 룩백 기간 저장 | 1시간 |

---

## 📋 일반 (월 내 완료)

| # | 작업 | 설명 | 예상 시간 |
|---|------|------|-----------|
| 10 | **장중 알림 유니버스 필터** | ETF 유니버스 종목만 표시 옵션 | 1시간 |
| 11 | **Live 파라미터 롤백 UI** | 이전 Live로 복원 (API 완료, UI 필요) | 1시간 |

---

## 🔮 향후 (Phase 1~4)

| Phase | 작업 | 설명 |
|-------|------|------|
| 1 | 장기 백테스트 검증 | 12개월+ / 2022~2024 하락장 검증 |
| 2 | Walk-Forward 프레임워크 | 롤링 윈도우 / PSS |
| 3 | TP/SL 고도화 | Take Profit / Trailing Stop |
| 4 | 고급 기능 | Market Breadth / 이벤트 캘린더 |

---

# 상세 구현 계획

---

## 1. Live 파라미터 구조 구현

### 목표
`optimal_params.json` 구조를 `live` / `live_history` / `research`로 분리

### 변경 전
```json
{
  "current": { ... },
  "history": [ ... ]
}
```

### 변경 후
```json
{
  "live": {
    "params": {
      "lookback": "3M",
      "ma_period": 40,
      "rsi_period": 9,
      "stop_loss": -20,
      "max_positions": 12
    },
    "promoted_at": "2025-12-11T01:20:00",
    "source_trial_id": 3,
    "notes": "AI 분석 후 승격"
  },
  "live_history": [
    { "params": {...}, "demoted_at": "...", "reason": "새 파라미터로 교체" }
  ],
  "research": [...]
}
```

### 작업 항목
- [ ] `OptimalParamsService` 수정: `promote_to_live()`, `load_live()`, `rollback_live()` 메서드 추가
- [ ] 기존 `current` → `live` 마이그레이션 스크립트
- [ ] `lookback` 파라미터 추가

---

## 2. "실전 파라미터로 적용" 버튼

### 목표
튜닝 UI에서 선택한 Trial을 Live로 승격

### UI 위치
```
[최적 파라미터 저장] [최적 결과 AI 분석] [실전 파라미터로 적용]
```

### API
```
POST /api/v1/optimal-params/promote-to-live
Body: { trial_id, params, notes }
```

### 작업 항목
- [ ] 백엔드 API 엔드포인트 추가
- [ ] 프론트엔드 버튼 및 확인 모달 추가
- [ ] 승격 시 이전 Live를 `live_history`로 이동

---

## 3. 일일 추천 알림 신규 개발

### 목표
Live 파라미터 기반 매수/매도 신호 생성 및 알림

### 알림 형식
```
📊 일일 추천 (2025-12-11)
(Momentum 3M / MA40 / RSI9 기준)

📥 매수 검토
  • KODEX 200: 목표 40.0%
  • KODEX 반도체: 목표 20.0%

📤 매도 검토
  • TIGER 차이나: 현재 15% → 목표 0%

🔧 현재 Live: 룩백 3M / MA 40 / RSI 9 / 손절 -20%
```

### 로직
```
1. Live 파라미터 로드 (lookback, ma_period, rsi_period, stop_loss)
2. 유니버스 필터링 (거래대금, 상장일)
3. 모멘텀 스코어 계산 (lookback + ma_period)
4. RSI 스케일링 (rsi_period)
5. 목표 비중 계산 (Equal-Weight or PyPortfolioOpt)
6. 현재 보유 비중과 비교
   - 부족 → 매수 검토
   - 초과/외부 → 매도 검토
7. 알림 발송
```

### 작업 항목
- [ ] `scripts/nas/daily_recommendation_alert.py` 신규 생성
- [ ] `core/strategy/signal_generator.py` 신규 생성 (신호 생성 로직)
- [ ] 크론 등록 (예: 08:30 장시작 전)

---

## 4. 기존 알림에 Live 파라미터 연동

### 알림별 연동 범위

| 알림 | 연동 수준 | 변경 내용 |
|------|-----------|-----------|
| **장시작 알림** | 요약만 | 하단에 한 줄 추가: `🔧 현재 전략: 룩백 3M / MA 40 / RSI 9 / 손절 -20%` |
| **일일 리포트** | 강하게 | 매매 신호 섹션에 Live 기반 신호 / 투자 전략 섹션에 파라미터 요약 |
| **주간 리포트** | 중간 | 손절 리스트에 합성 손절 / 다음 주 전략에 파라미터 요약 |
| **장중 알림** | 약하게 | (선택) ETF 유니버스 필터 옵션 |

### 작업 항목
- [ ] `market_open_alert.py`: Live 파라미터 요약 한 줄 추가
- [ ] `daily_report_alert.py`: 매매 신호 + 투자 전략 섹션 수정
- [ ] `weekly_report_alert.py`: 손절 리스트 + 다음 주 전략 수정
- [ ] `intraday_alert.py`: (선택) 유니버스 필터 옵션

---

## 5. 손절 합성 로직

### 목표
ETF와 개별주의 손절 기준 분리

### 로직
```python
def get_stop_loss(ticker, live_params, hybrid_matrix):
    if is_etf(ticker):
        # ETF: 전략 손절과 하이브리드 중 더 보수적인 값
        strategy_stop = live_params["stop_loss"]  # 예: -20%
        hybrid_stop = hybrid_matrix.get_stop(ticker)  # 예: -10%
        return max(strategy_stop, hybrid_stop)  # -10% (더 보수적)
    else:
        # 개별주: 하이브리드만 사용
        return hybrid_matrix.get_stop(ticker)
```

### 작업 항목
- [ ] `core/strategy/stop_loss_manager.py` 신규 생성
- [ ] 주간 리포트 손절 리스트에 적용
- [ ] 손절 모니터링 알림에 적용

---

## 6. lookback 파라미터 추가

### 목표
튜닝 시 사용한 룩백 기간을 Live 파라미터에 포함

### 변경
```json
"live": {
  "params": {
    "lookback": "3M",  // 추가
    "ma_period": 40,
    "rsi_period": 9,
    "stop_loss": -20
  }
}
```

### 작업 항목
- [ ] `TuningParams`에 `lookback_months` 이미 있음 → 저장 시 포함
- [ ] `OptimalParamsService.promote_to_live()`에서 lookback 저장
- [ ] 알림에서 lookback 표시

---

# 기구현 현황 (참고용)

## ✅ 완료된 항목

### 시스템 전반
- 데이터 수집 (pykrx)
- 데이터 캐싱 (Parquet)
- 유니버스 필터링
- 캘린더 가드 (휴장일)
- 로깅 시스템

### 전략
- 레짐 정의 (MA50/200)
- 모멘텀 스코어 (MAPS)
- RSI 비중 스케일링
- 백테스트 엔진/러너
- 스탑로스
- 방어 시스템

### 튜닝
- Optuna 튜닝
- Train/Val/Test 분할
- 과적합 경고
- 엔진 헬스체크
- pick_best_trial 알고리즘

### API/UI
- 백테스트 API
- 튜닝 UI
- AI 분석 프롬프트 생성

### 알림
- 장시작 알림
- 장중 알림
- 일일 리포트
- 주간 리포트
- 텔레그램/Slack

---

# 파라미터 생애주기

```
[튜닝 Trial] → Research (연구 결과)
     ↓
[AI 분석 + 사람 판단] → "이거 괜찮다"
     ↓
[실전 파라미터로 적용] → Live (운영 파라미터)
     ↓
[PUSH 스크립트] → 오직 Live만 사용
```

## 선별 조건 (pick_best_trial)

```python
def pick_best_trial(trials):
    valid = [
        t for t in trials
        if t["engine_health"]["is_valid"]      # 1. 엔진 정상
        and t["status"] == "정상"               # 2. 상태 정상
        and t["test"]["sharpe"] > 0            # 3. Test Sharpe 양수
        and t["test"]["sharpe"] >= t["train"]["sharpe"] * 0.7  # 4. 과적합 필터
    ]
    return max(valid, key=lambda t: t["test"]["sharpe"])
```

---

# 알림별 파라미터 사용 요약

| 알림 | 파라미터 사용 | 연동 수준 |
|------|---------------|-----------|
| **일일 추천** | lookback, ma_period, rsi_period, stop_loss | 🔴 강하게 |
| **일일 리포트 (매매 신호)** | lookback, ma_period, rsi_period | 🔴 강하게 |
| **일일 리포트 (투자 전략)** | 전체 요약 | 🔴 강하게 |
| **주간 리포트 (다음 주 전략)** | 전체 요약 | 🟡 중간 |
| **주간 리포트 (손절 리스트)** | stop_loss + 하이브리드 합성 | 🟡 중간 |
| **손절 모니터링** | stop_loss + 하이브리드 합성 | 🟡 중간 |
| **장시작 알림** | 요약 한 줄 | 🟢 약하게 |
| **장중 알림** | (선택) 유니버스 필터 | 🟢 약하게 |
