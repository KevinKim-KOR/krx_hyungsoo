# 튜닝/검증 체계 설계 문서

> **작성**: 2025-12-16  
> **Author**: 형수  
> **목적**: 튜닝 파라미터 스키마 표준화 및 검증 체계(Test 봉인, Walk-Forward, 목적함수) 설계

---

## 1. 배경 / 문제 정의

### 1.1 현재 상황

| 항목 | 현재 상태 |
|------|----------|
| 활성 튜닝 변수 | ma_period, rsi_period, stop_loss (3개) |
| 비활성 후보 | volatility_filter, market_breadth, rsi_overbought, rsi_oversold, rebalance_threshold |
| 튜닝 방식 | Optuna 범위 탐색 (단일 백테스트 아님) |
| 검증 분할 | Train 70% / Val 15% / Test 15% |

### 1.2 핵심 문제

**반복되는 패턴:**
```
Train Sharpe: 높음 (2.0+)
Val Sharpe: 0 이하 / 부진
Test Sharpe: 비정상적으로 높음 (1.5+)
```

**원인 분석:**
1. **Test 데이터 누수**: 튜닝 선택 시 Test 성과를 참조하면 사실상 Test로 최적화한 것
2. **단일 분할의 한계**: 특정 기간에 운 좋게 맞는 파라미터가 선택됨
3. **Val 구간 짧음**: 15%는 약 2~3개월, 노이즈에 취약
4. **목적함수 단순**: Sharpe만 최대화 → 과적합 유도

### 1.3 해결 방향

| 문제 | 해결책 |
|------|--------|
| Test 누수 | **Test 봉인** — 선택/정렬에 사용 금지 |
| 단일 분할 한계 | **미니 Walk-Forward** — 롤링 검증 |
| 과적합 | **복합 목적함수** — Val 기반 + 안정성 페널티 |
| 재현성 | **run_manifest** — 모든 실행 조건 저장 |

---

## 2. 설계 원칙

### 2.1 검증 봉인 원칙

```
┌─────────────────────────────────────────────────────────┐
│  Train (70%)  │  Val (15%)  │  Test (15%)              │
│               │             │                          │
│  학습/탐색    │  선택 기준  │  최종 보고서에서만 열람  │
│               │             │  (선택/정렬/최적화 금지) │
└─────────────────────────────────────────────────────────┘
```

**강제 규칙:**
- Optuna objective = Val 성과만 사용
- UI 결과 테이블 정렬 = Val Sharpe 기준
- Test 컬럼은 "최종 리포트" 탭에서만 표시

### 2.2 탐색 공간 제어 원칙

```
활성 변수 수 × 각 변수 step 수 = 탐색 공간
```

**예시:**
- ma_period: (200-20)/10 = 18개
- rsi_period: (30-5)/1 = 25개
- stop_loss: (20-5)/1 = 15개
- **총 조합: 18 × 25 × 15 = 6,750개**

변수 1개 추가 시 조합이 수십 배 증가 → **단계형 튜닝 필수**

### 2.3 재현성 원칙

모든 튜닝 실행은 다음을 저장:
- 실행 조건 (기간, 변수, 범위)
- 결과 (best_params, 분할별 성과)
- 환경 (코드 버전, 데이터 버전)

---

## 3. 파라미터 스키마 설계

### 3.1 스키마 표준 필드

```yaml
variable_schema:
  key: string                    # 고유 식별자 (예: ma_period)
  label_ko: string               # 한글 라벨 (예: 이동평균 기간)
  category: enum                 # trend | momentum | risk | market | execution
  enabled_default: boolean       # 기본 활성화 여부
  
  type: enum                     # int | float | bool | enum
  range:
    min: number
    max: number
  step: number
  distribution: enum             # uniform | loguniform | categorical
  
  dependency:                    # 의존성 (선택)
    requires: string[]           # 이 변수가 켜져 있어야 의미 있음
    condition: string            # 조건식 (예: "rsi_period > 0")
  
  constraints:                   # 제약 조건 (선택)
    - expression: string         # 예: "rsi_oversold < rsi_overbought"
      message: string
  
  recommended_by_lookback:       # 룩백별 추천 범위
    3M: { min: number, max: number }
    6M: { min: number, max: number }
    12M: { min: number, max: number }
  
  notes:
    strategic_meaning: string    # 전략적 의미
    risk_warning: string         # 리스크/부작용
    interaction: string[]        # 상호작용 변수
```

### 3.2 현재 변수 스키마 정의

#### 활성 변수 (3개)

```yaml
ma_period:
  key: ma_period
  label_ko: 이동평균 기간
  category: trend
  enabled_default: true
  type: int
  range: { min: 20, max: 200 }
  step: 10
  distribution: uniform
  dependency: null
  constraints: null
  recommended_by_lookback:
    3M: { min: 20, max: 60 }      # 단기: 빠른 반응
    6M: { min: 40, max: 120 }     # 중기: 균형
    12M: { min: 60, max: 200 }    # 장기: 노이즈 필터
  notes:
    strategic_meaning: |
      추세 판단 기준. 짧으면 민감(휩쏘↑), 길면 둔감(지연↑)
    risk_warning: |
      3M 룩백에서 MA=200은 데이터 부족으로 무의미
    interaction:
      - stop_loss (MA 길면 손절도 넓게 가져가야 함)

rsi_period:
  key: rsi_period
  label_ko: RSI 기간
  category: momentum
  enabled_default: true
  type: int
  range: { min: 5, max: 30 }
  step: 1
  distribution: uniform
  dependency: null
  constraints: null
  recommended_by_lookback:
    3M: { min: 5, max: 14 }
    6M: { min: 7, max: 21 }
    12M: { min: 10, max: 30 }
  notes:
    strategic_meaning: |
      모멘텀 강도 측정. 짧으면 과민, 길면 둔감
    risk_warning: |
      RSI 5는 노이즈에 취약, 30은 신호 지연
    interaction:
      - rsi_overbought, rsi_oversold (기간과 임계값 조합)

stop_loss:
  key: stop_loss
  label_ko: 손절 비율(%)
  category: risk
  enabled_default: true
  type: int
  range: { min: -20, max: -5 }
  step: 1
  distribution: uniform
  dependency: null
  constraints:
    - expression: "stop_loss < 0"
      message: "손절은 음수여야 합니다"
  recommended_by_lookback:
    3M: { min: -10, max: -5 }     # 단기: 타이트
    6M: { min: -15, max: -7 }     # 중기: 균형
    12M: { min: -20, max: -10 }   # 장기: 넓게
  notes:
    strategic_meaning: |
      손실 제한. 타이트하면 휩쏘 손절↑, 넓으면 큰 손실 허용
    risk_warning: |
      -5%는 변동성 높은 ETF에서 빈번한 손절 유발
    interaction:
      - volatility_filter (vol 높으면 손절 넓게)
      - ma_period (MA 길면 손절도 넓게)
```

#### 비활성 변수 (5개) — 켜는 조건 포함

```yaml
volatility_filter:
  key: volatility_filter
  label_ko: 변동성 임계값(%)
  category: risk
  enabled_default: false
  type: int
  range: { min: 10, max: 50 }
  step: 5
  distribution: uniform
  dependency: null
  constraints: null
  recommended_by_lookback:
    3M: { min: 15, max: 30 }
    6M: { min: 20, max: 40 }
    12M: { min: 25, max: 50 }
  notes:
    strategic_meaning: |
      20일 변동성이 임계값 초과 시 진입 제한.
      급등락 구간에서 휩쏘 방지.
    risk_warning: |
      너무 낮으면 대부분 진입 차단 → 기회 손실
      너무 높으면 필터 무의미
    interaction:
      - stop_loss (vol filter + 타이트 손절 = 과잉 방어)
  enable_condition: |
    ✅ 켜는 조건:
    - 변동성 장세(VIX 20+)에서 과도한 손절이 반복될 때
    - stop_loss가 -10% 이상 타이트할 때 보완용
    ❌ 끄는 조건:
    - 저변동성 장세에서는 필터가 거의 작동 안 함
    - 추세 추종 전략에서 초기 진입 기회 놓칠 수 있음

market_breadth:
  key: market_breadth
  label_ko: 상승 종목 비율 기준(%)
  category: market
  enabled_default: false
  type: int
  range: { min: 30, max: 70 }
  step: 5
  distribution: uniform
  dependency: null
  constraints: null
  recommended_by_lookback:
    3M: { min: 40, max: 60 }
    6M: { min: 35, max: 65 }
    12M: { min: 30, max: 70 }
  notes:
    strategic_meaning: |
      시장 전체의 상승 참여도. 높으면 강세장, 낮으면 약세장.
      레짐 판단 보조 지표.
    risk_warning: |
      Breadth 데이터 수집이 선행되어야 함 (현재 미구현)
      단일 ETF 전략에서는 효과 제한적
    interaction:
      - 레짐 로직과 연동 필요
  enable_condition: |
    ✅ 켜는 조건:
    - Breadth 데이터 파이프라인 구축 완료 후
    - 레짐 기반 전략 고도화 시
    ❌ 끄는 조건:
    - 데이터 없으면 의미 없음

rsi_overbought:
  key: rsi_overbought
  label_ko: RSI 과매수 기준
  category: momentum
  enabled_default: false
  type: int
  range: { min: 60, max: 90 }
  step: 5
  distribution: uniform
  dependency:
    requires: [rsi_period]
    condition: "rsi_period가 활성화되어 있어야 함"
  constraints:
    - expression: "rsi_overbought > rsi_oversold"
      message: "과매수 기준은 과매도보다 커야 합니다"
  recommended_by_lookback:
    3M: { min: 70, max: 80 }
    6M: { min: 65, max: 85 }
    12M: { min: 60, max: 90 }
  notes:
    strategic_meaning: |
      RSI가 이 값 초과 시 과매수로 판단 → 매도 신호 또는 진입 제한
    risk_warning: |
      너무 낮으면(60) 강한 추세에서 조기 청산
      너무 높으면(90) 신호 거의 안 나옴
    interaction:
      - rsi_period (기간 짧으면 overbought도 높게)
      - rsi_oversold (쌍으로 튜닝)
  enable_condition: |
    ✅ 켜는 조건:
    - 역추세/평균회귀 전략 테스트 시
    - RSI 기반 청산 로직 추가 시
    ❌ 끄는 조건:
    - 순수 추세 추종에서는 오히려 방해

rsi_oversold:
  key: rsi_oversold
  label_ko: RSI 과매도 기준
  category: momentum
  enabled_default: false
  type: int
  range: { min: 10, max: 40 }
  step: 5
  distribution: uniform
  dependency:
    requires: [rsi_period]
    condition: "rsi_period가 활성화되어 있어야 함"
  constraints:
    - expression: "rsi_oversold < rsi_overbought"
      message: "과매도 기준은 과매수보다 작아야 합니다"
  recommended_by_lookback:
    3M: { min: 20, max: 30 }
    6M: { min: 15, max: 35 }
    12M: { min: 10, max: 40 }
  notes:
    strategic_meaning: |
      RSI가 이 값 미만 시 과매도로 판단 → 매수 신호
    risk_warning: |
      너무 높으면(40) 하락 추세에서 조기 진입
      너무 낮으면(10) 신호 거의 안 나옴
    interaction:
      - rsi_period, rsi_overbought
  enable_condition: |
    ✅ 켜는 조건:
    - 역추세/평균회귀 전략 테스트 시
    - rsi_overbought와 함께 활성화
    ❌ 끄는 조건:
    - 단독 사용 비권장

rebalance_threshold:
  key: rebalance_threshold
  label_ko: 리밸런싱 임계값(%)
  category: execution
  enabled_default: false
  type: float
  range: { min: 0.5, max: 5.0 }
  step: 0.5
  distribution: uniform
  dependency: null
  constraints: null
  recommended_by_lookback:
    3M: { min: 1.0, max: 3.0 }
    6M: { min: 0.5, max: 4.0 }
    12M: { min: 0.5, max: 5.0 }
  notes:
    strategic_meaning: |
      목표 비중 대비 괴리가 이 값 초과 시에만 리밸런싱.
      잦은 거래 방지, 비용 절감.
    risk_warning: |
      너무 높으면 비중 괴리 방치
      너무 낮으면 거래 빈번 → 비용 증가
    interaction:
      - 거래비용 가정과 연동
  enable_condition: |
    ✅ 켜는 조건:
    - 거래비용 민감도 분석 시
    - 실전 운용 시뮬레이션 시
    ❌ 끄는 조건:
    - 초기 전략 탐색 단계에서는 복잡도만 증가
```

---

## 4. 튜닝 단계(Phase) 설계

### 4.1 단계형 튜닝 구조

```
┌─────────────────────────────────────────────────────────────┐
│  Phase 1: 핵심 변수 탐색 (Discovery)                        │
│  - 변수: ma_period, rsi_period, stop_loss                   │
│  - 목적: 전략의 기본 골격 파악                              │
│  - Trials: 50~100                                           │
│  - 탐색 공간: ~6,750 조합                                   │
├─────────────────────────────────────────────────────────────┤
│  Phase 2: 필터 변수 추가 (Stability)                        │
│  - 변수: Phase 1 best + volatility_filter                   │
│  - 목적: 리스크 관리 강화                                   │
│  - Trials: 30~50                                            │
│  - 핵심 변수는 좁은 범위로 고정                             │
├─────────────────────────────────────────────────────────────┤
│  Phase 3: 정밀 조정 (Refine)                                │
│  - 변수: Phase 2 best 주변 ±10%                             │
│  - 목적: 미세 조정                                          │
│  - Trials: 20~30                                            │
│  - step 축소 (예: ma_period step=5)                         │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 Coarse-to-Fine 전략

**1차 (Coarse):**
```yaml
ma_period: { min: 20, max: 200, step: 20 }   # 9개
rsi_period: { min: 5, max: 30, step: 5 }     # 5개
stop_loss: { min: -20, max: -5, step: 5 }    # 3개
# 총 조합: 9 × 5 × 3 = 135개
```

**2차 (Fine):**
```yaml
# 1차 best가 ma=60, rsi=14, sl=-10이라면:
ma_period: { min: 40, max: 80, step: 5 }     # 8개
rsi_period: { min: 10, max: 18, step: 1 }    # 8개
stop_loss: { min: -12, max: -8, step: 1 }    # 4개
# 총 조합: 8 × 8 × 4 = 256개
```

### 4.3 UI 프리셋 설계

| 프리셋 | 변수 수 | Trials | 예상 시간 | 용도 |
|--------|---------|--------|-----------|------|
| **탐색 (Discovery)** | 3개 (핵심) | 50 | ~5분 | 초기 탐색 |
| **안정화 (Stability)** | 4개 (+필터) | 30 | ~3분 | 리스크 보완 |
| **정밀 (Refine)** | 3개 (좁은 범위) | 20 | ~2분 | 미세 조정 |
| **커스텀** | 사용자 선택 | 사용자 입력 | 계산 표시 | 고급 |

---

## 5. 검증 설계

### 5.1 Test 봉인 설계

#### 로직 레벨

```python
# Optuna objective 함수
def objective(trial):
    params = suggest_params(trial)
    result = run_backtest(params)
    
    # ✅ Val 성과만 반환 (Test 사용 금지)
    return result.val_sharpe

# 결과 정렬
def get_top_trials(trials):
    # ✅ Val 기준 정렬
    return sorted(trials, key=lambda t: t.val_sharpe, reverse=True)
```

#### UI 레벨

**결과 테이블 (튜닝 중/직후):**
| Trial | MA | RSI | SL | Train Sharpe | Val Sharpe | Test |
|-------|----|----|-----|--------------|------------|------|
| 1 | 60 | 14 | -10 | 2.1 | 1.5 | 🔒 |
| 2 | 80 | 12 | -8 | 1.9 | 1.3 | 🔒 |

**최종 리포트 (선택 후):**
| 항목 | 값 |
|------|-----|
| 선택 Trial | #1 |
| Train Sharpe | 2.1 |
| Val Sharpe | 1.5 |
| **Test Sharpe** | **1.8** ← 여기서만 공개 |

#### 문구 설계

```
⚠️ Test 성과는 최종 선택 후에만 확인할 수 있습니다.
   튜닝 중 Test를 참조하면 과적합 위험이 높아집니다.
```

### 5.2 미니 Walk-Forward 설계

**목표:** 정식 Walk-Forward 프레임워크 개발 전, 최소한의 롤링 검증

#### 구조

```
전체 기간: 2024-01-01 ~ 2025-12-31 (24개월)

롤링 윈도우 (stride=2개월):
┌──────────────────────────────────────────────────────────┐
│ Window 1: Train 18M │ Val 3M │ Test 3M                   │
│           2024-01 ~ 2025-06 │ 07~09 │ 10~12              │
├──────────────────────────────────────────────────────────┤
│ Window 2: Train 18M │ Val 3M │ Test 3M                   │
│           2024-03 ~ 2025-08 │ 09~11 │ 12~02              │
├──────────────────────────────────────────────────────────┤
│ Window 3: ...                                            │
└──────────────────────────────────────────────────────────┘
```

#### 집계 지표

```yaml
mini_walkforward_result:
  windows_count: 5
  sharpe:
    mean: 1.2
    std: 0.4
    min: 0.6
    max: 1.8
  cagr:
    mean: 15%
    std: 8%
  mdd:
    mean: -12%
    worst: -18%
  
  # 안정성 점수
  stability_score: 1.5   # = sharpe_mean / (sharpe_std + 0.1)
  
  # 승률
  win_rate: 80%          # sharpe > 0인 윈도우 비율
```

#### 안정성 점수 공식

```python
def stability_score(sharpe_list, k=1.0, epsilon=0.1):
    """
    높을수록 안정적
    - sharpe_mean이 높고
    - sharpe_std가 낮을수록 좋음
    """
    mean = np.mean(sharpe_list)
    std = np.std(sharpe_list)
    return mean / (std + epsilon)

# 또는 하방 리스크 반영
def stability_score_downside(sharpe_list, k=1.0):
    """
    평균 - k * 표준편차
    """
    return np.mean(sharpe_list) - k * np.std(sharpe_list)
```

### 5.3 목적함수(Objective) 설계

#### Option A: Val Sharpe + MDD 페널티

```python
def objective_with_mdd_penalty(trial):
    result = run_backtest(trial)
    
    val_sharpe = result.val_sharpe
    val_mdd = abs(result.val_mdd)  # 양수로 변환
    
    # MDD가 15% 초과 시 페널티
    mdd_penalty = max(0, val_mdd - 15) * 0.1
    
    return val_sharpe - mdd_penalty
```

#### Option B: 안정성 페널티 (구간별 분산)

```python
def objective_with_stability(trial):
    result = run_backtest(trial)
    
    # 월별 수익률 분산
    monthly_returns = result.monthly_returns
    volatility_penalty = np.std(monthly_returns) * 0.5
    
    return result.val_sharpe - volatility_penalty
```

#### Option C: 거래비용 반영

```python
def objective_with_costs(trial):
    result = run_backtest(trial)
    
    # 거래비용 차감 후 Sharpe
    net_sharpe = result.val_sharpe_after_costs
    
    return net_sharpe
```

#### Option D: Multi-Objective (Pareto)

```python
# Optuna multi-objective
def objective_multi(trial):
    result = run_backtest(trial)
    
    return (
        result.val_sharpe,      # 최대화
        -abs(result.val_mdd),   # 최소화 (음수로)
        -result.turnover        # 최소화 (음수로)
    )

# Pareto front에서 사용자가 선택
```

**UI 표시:**
```
┌─────────────────────────────────────────────────────────┐
│  Pareto Front (최적 조합들)                             │
│                                                         │
│  ● Trial 1: Sharpe 1.8, MDD -10%, Turnover 12회        │
│  ● Trial 5: Sharpe 1.5, MDD -7%, Turnover 8회          │
│  ● Trial 9: Sharpe 2.0, MDD -15%, Turnover 18회        │
│                                                         │
│  → 리스크 선호도에 따라 선택하세요                      │
└─────────────────────────────────────────────────────────┘
```

---

## 6. UI/UX 변경안

### 6.1 튜닝 변수 설정 영역 개선

#### 현재 → 개선

**현재:**
- 변수 목록이 평면적으로 나열
- 활성/비활성 토글만 존재
- 범위/step 정보만 표시

**개선:**

```
┌─────────────────────────────────────────────────────────────┐
│  튜닝 변수 설정                                             │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│                                                             │
│  📊 활성 변수: 3개 | 예상 탐색 공간: ~6,750 조합            │
│  ⏱️ 예상 시간: 50 trials × 3초 = ~2.5분                    │
│                                                             │
│  ▼ 추세 (Trend)                                            │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ✅ ma_period (이동평균 기간)                        │   │
│  │    범위: [20, 200] step=10                          │   │
│  │    💡 3M 추천: 20~60 | 현재 룩백: 3M               │   │
│  │    ⓘ 짧으면 민감(휩쏘↑), 길면 둔감(지연↑)         │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ▼ 모멘텀 (Momentum)                                       │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ✅ rsi_period (RSI 기간)                            │   │
│  │    범위: [5, 30] step=1                             │   │
│  │    💡 3M 추천: 5~14                                 │   │
│  └─────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ☐ rsi_overbought (RSI 과매수)                      │   │
│  │    ⚠️ rsi_period 활성화 필요                       │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ▼ 리스크 (Risk)                                           │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ✅ stop_loss (손절 비율)                            │   │
│  │    범위: [-20, -5] step=1                           │   │
│  └─────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ☐ volatility_filter (변동성 필터)                  │   │
│  │    💡 켜는 조건: 변동성 장세에서 휩쏘 방지 시      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ▶ 시장 (Market) — 접힘                                    │
│  ▶ 실행 (Execution) — 접힘                                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 6.2 자동 튜닝(Optuna) 영역 개선

```
┌─────────────────────────────────────────────────────────────┐
│  자동 튜닝 (Optuna)                        [범위 기반 탐색] │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│                                                             │
│  📅 백테스트 기간                                          │
│  ┌──────────────┐    ┌──────────────┐                      │
│  │ 2024-01-01   │ ~  │ 2025-12-31   │                      │
│  └──────────────┘    └──────────────┘                      │
│                                                             │
│  📊 룩백 기간 (멀티 선택)                                  │
│  [3개월 ✓] [6개월 ✓] [12개월 ✓]                           │
│                                                             │
│  🎯 튜닝 모드                                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ○ 탐색 (Discovery)  — 50 trials, 넓은 범위          │   │
│  │ ● 안정화 (Stability) — 30 trials, 필터 추가         │   │
│  │ ○ 정밀 (Refine)     — 20 trials, 좁은 범위          │   │
│  │ ○ 커스텀            — 직접 설정                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ⚙️ Trials 수: [30 ▼]                                      │
│  ⏱️ 예상 시간: ~3분 (30 trials × 6초)                      │
│                                                             │
│  💡 자동 튜닝이란?                                         │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 현재 Live 값(예: MA=20)으로 단일 백테스트를         │   │
│  │ 실행하는 것이 아닙니다.                             │   │
│  │ 위에서 설정한 범위 내에서 Optuna가 최적 조합을      │   │
│  │ 자동으로 탐색합니다.                                │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  [🚀 튜닝 시작]                                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 6.3 결과 화면 (Test 봉인 UX)

**튜닝 결과 테이블:**

```
┌─────────────────────────────────────────────────────────────┐
│  튜닝 결과 (Val 기준 정렬)                                  │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│                                                             │
│  ⚠️ 선택 기준은 Val 성과입니다. Test는 최종 확정 후 공개.  │
│                                                             │
│  ┌────┬────┬────┬────┬────────┬────────┬──────┬─────────┐  │
│  │ #  │ MA │RSI │ SL │ Train  │  Val   │ Test │ 상태    │  │
│  ├────┼────┼────┼────┼────────┼────────┼──────┼─────────┤  │
│  │ 1  │ 60 │ 14 │-10 │  2.1   │  1.5   │  🔒  │ ✅ 정상 │  │
│  │ 2  │ 80 │ 12 │ -8 │  1.9   │  1.3   │  🔒  │ ✅ 정상 │  │
│  │ 3  │ 40 │ 21 │-15 │  2.5   │  0.2   │  🔒  │ ⚠️ 과적합│  │
│  └────┴────┴────┴────┴────────┴────────┴──────┴─────────┘  │
│                                                             │
│  [Trial #1 선택하여 Test 확인] [AI 분석]                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**최종 리포트 (선택 후):**

```
┌─────────────────────────────────────────────────────────────┐
│  📋 최종 리포트 — Trial #1                                  │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│                                                             │
│  파라미터                                                   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ MA Period: 60 | RSI Period: 14 | Stop Loss: -10%    │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  성과 요약                                                  │
│  ┌─────────────┬─────────┬─────────┬─────────┐            │
│  │             │  Train  │   Val   │  Test   │            │
│  ├─────────────┼─────────┼─────────┼─────────┤            │
│  │ Sharpe      │   2.1   │   1.5   │  1.8 ✓  │            │
│  │ CAGR        │  25%    │  18%    │  20%    │            │
│  │ MDD         │  -8%    │  -12%   │  -10%   │            │
│  └─────────────┴─────────┴─────────┴─────────┘            │
│                                                             │
│  ✅ Test 성과가 Val과 유사 → 과적합 위험 낮음              │
│                                                             │
│  [Live 적용] [AI 분석] [리포트 저장]                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 7. 저장/리포팅 설계

### 7.1 run_manifest 스키마

```json
{
  "run_id": "tuning_20251216_143052_abc123",
  "created_at": "2025-12-16T14:30:52+09:00",
  
  "config": {
    "period": {
      "start_date": "2024-01-01",
      "end_date": "2025-12-31"
    },
    "lookbacks": [3, 6, 12],
    "trials": 50,
    "objective": "val_sharpe_with_mdd_penalty",
    
    "variables": {
      "ma_period": {
        "enabled": true,
        "range": [20, 200],
        "step": 10
      },
      "rsi_period": {
        "enabled": true,
        "range": [5, 30],
        "step": 1
      },
      "stop_loss": {
        "enabled": true,
        "range": [-20, -5],
        "step": 1
      }
    },
    
    "cost_assumptions": {
      "commission_rate": 0.00015,
      "slippage_rate": 0.001
    }
  },
  
  "results": {
    "best_trial": {
      "trial_number": 1,
      "params": {
        "ma_period": 60,
        "rsi_period": 14,
        "stop_loss": -10
      },
      "metrics": {
        "train": { "sharpe": 2.1, "cagr": 0.25, "mdd": -0.08 },
        "val": { "sharpe": 1.5, "cagr": 0.18, "mdd": -0.12 },
        "test": { "sharpe": 1.8, "cagr": 0.20, "mdd": -0.10 }
      }
    },
    "all_trials_count": 50,
    "convergence_trial": 35
  },
  
  "environment": {
    "code_version": "git:abc123def",
    "data_version": "ohlcv_20251216",
    "python_version": "3.11.5",
    "optuna_version": "3.4.0"
  }
}
```

### 7.2 저장 위치

```
data/tuning_history/
├── tuning_20251216_143052_abc123/
│   ├── manifest.json           # 실행 조건 + 결과 요약
│   ├── all_trials.parquet      # 전체 Trial 결과
│   ├── best_params.json        # 최적 파라미터만
│   └── report.md               # 자동 생성 리포트
```

### 7.3 리포트 템플릿

```markdown
# 튜닝 리포트

**Run ID**: tuning_20251216_143052_abc123  
**실행 시각**: 2025-12-16 14:30:52

## 1. 실행 조건

| 항목 | 값 |
|------|-----|
| 백테스트 기간 | 2024-01-01 ~ 2025-12-31 |
| 룩백 | 3M, 6M, 12M |
| Trials | 50 |
| 목적함수 | Val Sharpe + MDD 페널티 |

### 활성 변수

| 변수 | 범위 | Step |
|------|------|------|
| ma_period | 20~200 | 10 |
| rsi_period | 5~30 | 1 |
| stop_loss | -20~-5 | 1 |

## 2. 최적 결과

### 파라미터
- MA Period: **60**
- RSI Period: **14**
- Stop Loss: **-10%**

### 성과

| 구간 | Sharpe | CAGR | MDD |
|------|--------|------|-----|
| Train | 2.1 | 25% | -8% |
| Val | 1.5 | 18% | -12% |
| Test | 1.8 | 20% | -10% |

## 3. 검증 결과

- ✅ Val → Test 성과 유지 (과적합 위험 낮음)
- ✅ MDD 15% 미만
- ⚠️ Train/Val 괴리 존재 (2.1 → 1.5)

## 4. 권고

- **실전 적용 가능** (단, 모니터링 필요)
- 추가 검증 권장: Walk-Forward 분석
```

---

## 8. 리스크 / 트레이드오프

| 설계 결정 | 장점 | 단점/리스크 |
|-----------|------|-------------|
| **Test 봉인** | 과적합 방지 | 사용자 불편 (결과 즉시 못 봄) |
| **Val 기준 선택** | 일반화 성능 중시 | Val 구간이 짧으면 노이즈에 취약 |
| **단계형 튜닝** | 탐색 공간 제어 | 단계 간 전환 UX 복잡 |
| **미니 Walk-Forward** | 안정성 검증 | 계산 시간 증가 (윈도우 수 × 시간) |
| **복합 목적함수** | 다면 평가 | 가중치 설정 주관적 |
| **run_manifest 저장** | 재현성 보장 | 저장 공간 증가 |

### 완화 방안

| 리스크 | 완화 방안 |
|--------|----------|
| Test 봉인 불편 | "선택 확정" 버튼으로 명시적 전환 |
| Val 노이즈 | 미니 Walk-Forward로 보완 |
| 단계 전환 복잡 | 프리셋 모드로 자동화 |
| 계산 시간 | 윈도우 수 제한 (3~5개) |

---

## 9. 다음 액션 (구현 우선순위)

### Phase 0: 즉시 적용 (1~2일)

1. **Test 봉인 UI 적용**
   - 결과 테이블에서 Test 컬럼 숨김 (🔒 표시)
   - "선택 확정" 버튼 추가
   - 경고 문구 추가

2. **Objective 함수 변경**
   - 현재: `return test_sharpe` → 변경: `return val_sharpe`
   - MDD 페널티 옵션 추가

### Phase 1: 단기 (1주)

3. **변수 스키마 YAML 작성**
   - `config/tuning_variables.yaml` 신규
   - 위 스키마대로 모든 변수 정의

4. **UI 카테고리 분류**
   - 변수를 카테고리별 접기/펼치기
   - 활성 변수 count 표시
   - 탐색 공간 크기 계산 표시

5. **튜닝 모드 프리셋**
   - Discovery / Stability / Refine 프리셋 버튼
   - 각 프리셋별 변수/trials 자동 설정

### Phase 2: 중기 (2~3주)

6. **미니 Walk-Forward 구현**
   - 윈도우 생성기
   - 롤링 백테스트 실행
   - 안정성 점수 계산

7. **run_manifest 저장**
   - 실행 시 자동 저장
   - 히스토리 조회 UI

### Phase 3: 장기 (1개월+)

8. **Multi-Objective 지원**
   - Pareto front 시각화
   - 사용자 선택 UI

9. **정식 Walk-Forward Framework**
   - 기존 PLAN_FUTURE_WORK.md의 Phase 2와 통합

---

## 부록: 용어 정리

| 용어 | 설명 |
|------|------|
| **Train** | 파라미터 학습/탐색에 사용하는 구간 (70%) |
| **Val (Validation)** | 파라미터 선택 기준이 되는 구간 (15%) |
| **Test** | 최종 성과 확인용, 선택에 사용 금지 (15%) |
| **Walk-Forward** | 롤링 윈도우로 여러 시점에서 검증 |
| **Objective** | Optuna가 최적화하는 목적함수 |
| **Pareto Front** | 다목적 최적화에서 최적 해 집합 |
| **PSS** | Parameter Stability Score (파라미터 안정성 점수) |
