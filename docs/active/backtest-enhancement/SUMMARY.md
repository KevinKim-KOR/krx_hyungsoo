# 백테스트 엔진 고도화 - 요약 및 실행 계획

**작성일**: 2025-11-29  
**목적**: 전체 설계 요약 및 실행 가이드

---

## 📊 핵심 요약

### 현황 진단

#### 이미 잘 구현된 부분
1. **거래비용 모델**: `commission_rate`, `slippage_rate` 파라미터화
2. **레짐 엔진**: `MarketRegimeDetector` 완성 (Week 3 검증)
3. **성과 지표**: `get_performance_metrics()` 구현
4. **거래 기록**: `portfolio.trades` 리스트

#### 개선 필요한 부분 (우선순위순)

| 순위 | 단계 | 필요성 | 난이도 | 예상 기간 | 비고 |
|------|------|--------|--------|----------|------|
| **0** | 간단한 Train/Test 분리 | ⭐⭐⭐⭐⭐ | ⭐ | 0.5일 | **검증 프레임워크** |
| **1** | 거래비용 모델 (상품별) | ⭐⭐⭐⭐⭐ | ⭐⭐ | 1~2일 | ETF/주식 구분 |
| **2** | 레짐 노출 스케일링 | ⭐⭐⭐⭐⭐ | ⭐⭐ | 1~2일 | 레버리지 처리 |
| **3** | Train/Val/Test 고도화 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 2~3일 | 단계적 튜닝 |
| **4** | Config 연결 | ⭐⭐⭐ | ⭐ | 0.5일 | - |
| **5** | 분석 로그 | ⭐⭐⭐⭐ | ⭐⭐ | 1일 | - |

**총 예상 기간**: 6~9일

**핵심 변경**: Phase 0 추가 - 모든 변경사항을 Train/Test 양쪽에서 검증

---

## 단계별 실행 계획

### Phase 0: 검증 프레임워크 (0.5일) 

**목표**: 이후 모든 변경사항을 Train/Test 양쪽에서 검증

#### 구현 내용
```python
# 간단한 Train/Test 분리 (70/30)
def simple_train_test_split(start_date, end_date):
    total_days = (end_date - start_date).days
    train_days = int(total_days * 0.7)
    
    train_end = start_date + timedelta(days=train_days)
    test_start = train_end + timedelta(days=1)
    
    return (start_date, train_end), (test_start, end_date)

# 백테스트 함수
def run_backtest_with_split(price_data, ...):
    (train_start, train_end), (test_start, test_end) = simple_train_test_split(...)
    
    # Train
    train_results = runner.run(price_data, train_start, train_end, ...)
    
    # Test
    test_results = runner.run(price_data, test_start, test_end, ...)
    
    return {
        'train': train_results,
        'test': test_results
    }
```

#### 검증 기준
```python
✅ Train 성과 > Test 성과 (정상)
✅ Test 성과가 너무 낮지 않음 (과적합 아님)
```

---

### 1단계: 거래비용 모델 고도화 (1~2일)

**목표**: "이제부터는 무조건 Net 기준으로만 성과를 본다"

#### 구현 내용
1. **거래세 추가 (상품별 차별화)**
   - 주식/리츠: 0.23% 거래세
   - ETF: 0% (면제)
   - 레버리지 ETF: 0% (면제)
   - `instrument_type` 파라미터 추가

2. **Gross/Net 구분**
   - `track_gross_metrics=True` 플래그
   - `cagr_gross`, `cagr_net`, `sharpe_gross`, `sharpe_net`
   - `cost_drag_annual` (연간 비용 영향)

3. **거래 요약 통계**
   - `total_turnover` (연율화 회전율)
   - `avg_holding_period` (평균 보유 기간)

#### 예상 성과 변화
```
현재 (거래세 미반영):
- CAGR: 27.05%

거래세 반영 후 (ETF 전략):
- CAGR: 27.0% (거의 변화 없음, ETF는 면제)
- Cost Drag: -0.05% (수수료만)

거래세 반영 후 (주식 전략):
- CAGR: 26.0~26.5% (약 0.5~1% 감소)
- Cost Drag: -0.5~1% (연간)
```

#### 테스트 기준
```
✅ instrument_type='etf' → 거래세 0%
✅ instrument_type='stock' → 거래세 0.23%
✅ Train/Test 양쪽에서 검증
✅ Gross > Net (항상)
✅ Cost Impact = (Gross - Net) / Initial Capital
```

---

### 2단계: 레짐 노출 스케일링 (1~2일)

**목표**: "레짐 점수에 따라 비중을 계단식으로 조절"

#### 현재 문제
```python
# ❌ 레짐 감지는 하지만 사용 안 함!
regime, confidence = self.regime_detector.detect_regime(...)
target_weights = self._generate_target_weights(...)
self.engine.rebalance(target_weights, ...)  # 항상 100% 포지션
```

#### 해결 방법
```python
# ✅ 레짐 비율 적용
position_ratio = self.regime_detector.get_position_ratio(regime, confidence)
adjusted_weights = self._apply_regime_scaling(target_weights, position_ratio)
self.engine.rebalance(adjusted_weights, ...)
```

#### 예상 성과 변화
```
현재 (레짐 미반영):
- Bull 레짐: 100% 포지션
- Bear 레짐: 100% 포지션  ← 문제!
- MDD: -19.92%
- Week 3 성과는 레짐 덕이 아님

레짐 반영 후 (레버리지 비활성화):
- Bull 레짐: 100% 포지션
- Bear 레짐: 40% 포지션  ← 방어!
- MDD: -15% (예상, 약 25% 개선)
- Sharpe: 1.6~1.7 (예상, 증가)

레짐 반영 후 (레버리지 활성화):
- Bull 레짐: 120% 포지션 (위험!)
- Bear 레짐: 40% 포지션
- MDD: -12% (예상, 하지만 위험도 증가)
```

#### 테스트 기준
```
✅ Bull 레짐 → 비중 합 ≤ 1.0 (레버리지 비활성화 시)
✅ Bear 레짐 → 비중 합 < 0.6
✅ Train/Test 양쪽에서 MDD 개선 확인
✅ Sharpe 유지 또는 증가
✅ 현금 음수 발생 안 함
```

---

### 3단계: Train/Val/Test 파이프라인 (2~3일)

**목표**: "과최적화 방지 + 실전 성과 예측"

#### 구현 내용
1. **데이터 분할** (시간순)
   - Train: 70% (파라미터 탐색)
   - Validation: 15% (최적 파라미터 선택)
   - Test: 15% (실전 성과 예측)

2. **파라미터 그리드 서치**
   - Train에서 모든 조합 테스트
   - Validation에서 최적 파라미터 선택

3. **결과 비교**
   - Train/Val/Test 성과 비교
   - 과최적화 여부 확인

#### 예상 결과
```
전체 기간 백테스트 (현재):
- Sharpe: 1.51 (과최적화 가능성)

Train/Val/Test 분리 후:
- Train Sharpe: 1.6 (최적화)
- Val Sharpe: 1.5 (검증)
- Test Sharpe: 1.4 (실전 예측, 신뢰도 높음)
```

#### 테스트 기준
```
✅ Train > Val > Test (일반적 패턴)
✅ Test 성과가 너무 낮지 않음 (과최적화 아님)
✅ Val과 Test 차이가 크지 않음 (안정적)
```

---

### 4단계: Config 연결 (0.5일)

**목표**: "함수 시그니처는 그대로, Config만 바꾸는 구조"

#### 구현 내용
```python
# 기존 방식 (하드코딩)
engine = BacktestEngine(
    initial_capital=10000000,
    commission_rate=0.00015,
    ...
)

# 새 방식 (Config)
engine = create_backtest_engine_from_config()

# ✅ 하위 호환성 유지!
```

#### Config 구조
```yaml
# config/backtest.yaml
backtest:
  initial_capital: 10000000
  max_positions: 10
  costs:
    korea:
      commission_rate: 0.00015
      tax_rate: 0.0023
      slippage_bps: 5
```

---

### 5단계: 분석 로그 추가 (1일)

**목표**: "일자별/트레이드별 상세 로그"

#### 구현 내용
1. **일자별 로그**
   ```python
   daily_logs = pd.DataFrame({
       'date': [...],
       'portfolio_value': [...],
       'regime': ['bull', 'bear', ...],
       'equity_exposure': [0.8, 0.4, ...],
       'cash_exposure': [0.2, 0.6, ...]
   })
   ```

2. **트레이드 로그**
   ```python
   trade_logs = pd.DataFrame({
       'date': [...],
       'ticker': [...],
       'side': ['BUY', 'SELL', ...],
       'qty': [...],
       'price_gross': [...],
       'price_net': [...],
       'fee': [...],
       'slippage': [...]
   })
   ```

---

## 📝 구현 체크리스트

### Phase 0: 검증 프레임워크
- [ ] `simple_train_test_split()` 함수 구현
- [ ] `run_backtest_with_split()` 함수 구현
- [ ] 기존 백테스트를 Train/Test로 분리 실행
- [ ] 결과 비교 리포트 생성

### 1단계: 거래비용 모델
- [ ] `config/backtest.yaml` 작성
- [ ] `Trade` 클래스에 `tax`, `slippage` 필드 추가
- [ ] `execute_sell()`에 거래세 계산 추가
- [ ] `execute_buy()`에 상세 로그 추가
- [ ] `get_performance_metrics()`에 Gross/Net 구분 추가
- [ ] `_calculate_turnover()` 구현
- [ ] `_calculate_avg_holding_period()` 구현
- [ ] 단위 테스트 작성 (`tests/test_backtest_costs.py`)
- [ ] 통합 테스트 (기존 백테스트 재실행)

### 2단계: 레짐 노출 스케일링
- [ ] `BacktestRunner.__init__()`에 `regime_detector` 추가
- [ ] `BacktestRunner.run()`에 레짐 감지 로직 추가
- [ ] `_apply_regime_scaling()` 메서드 구현
- [ ] `MarketRegimeDetector`에 `enable_leverage` 플래그 추가 (기본 False)
- [ ] 레버리지 비활성화 시 비중 범위 조정 (0.8~1.0)
- [ ] 현금 음수 방지 로직 추가
- [ ] 단위 테스트 작성 (`tests/test_regime_scaling.py`)
- [ ] **Train/Test 양쪽에서 통합 테스트 (MDD 개선 확인)**

### 3단계: Train/Val/Test 고도화
- [ ] `extensions/backtest/train_val_test.py` 생성
- [ ] `TrainValTestSplitter` 클래스 구현 (최소 길이 검증)
- [ ] `validate_regime_balance()` 함수 구현
- [ ] `ParameterGridSearch` 클래스 구현 (단계적 튜닝)
- [ ] `detect_validation_overfit()` 함수 구현
- [ ] `TrainValTestPipeline` 클래스 구현
- [ ] 실행 스크립트 작성 (`scripts/dev/phase3/run_train_val_test.py`)
- [ ] 통합 테스트

### 4단계: Config 연결
- [ ] `create_backtest_engine_from_config()` 함수 구현
- [ ] `config/backtest.yaml` 완성
- [ ] 하위 호환성 테스트

### 5단계: 분석 로그
- [ ] `BacktestEngine.__init__()`에 `daily_logs` 추가
- [ ] `update_nav()`에 일자별 로그 추가
- [ ] `get_performance_metrics()`에 로그 반환 추가
- [ ] 로그 DataFrame 변환 함수 작성
- [ ] 시각화 함수 작성 (선택)

---

## 🚀 실행 순서

### Phase 1: 기초 작업 (2~3일)
1. **1단계: 거래비용 모델** (1~2일)
   - 가장 영향도 높음
   - 다른 단계의 기반

2. **4단계: Config 연결** (0.5일)
   - 간단하고 빠름
   - 이후 단계에서 활용

### Phase 2: 핵심 기능 (2~3일)
3. **2단계: 레짐 노출 스케일링** (1~2일)
   - MDD 개선 핵심
   - 즉시 효과 확인 가능

### Phase 3: 고급 기능 (2~3일)
4. **3단계: Train/Val/Test** (2~3일)
   - 가장 복잡
   - 별도 모듈로 분리

### Phase 4: 마무리 (1일)
5. **5단계: 분석 로그** (1일)
   - 디버깅 및 분석용
   - 선택적

---

## 📊 예상 최종 성과

### 현재 (Week 3)
```
CAGR: 27.05%
Sharpe: 1.51
MDD: -19.92%
```

### 개선 후 (예상)
```
CAGR: 26.0~26.5% (거래세 반영)
Sharpe: 1.6~1.7 (레짐 스케일링)
MDD: -15% 이하 (레짐 스케일링)

Cost Drag: -0.5~1% (연간)
Turnover: 3~5 (연율화)
Avg Holding: 20~30일
```

### 신뢰도 향상
```
Train/Val/Test 분리로:
- 과최적화 방지
- 실전 성과 예측 가능
- 파라미터 안정성 확보
```

---

## ⚠️ 주의사항

### 검증 프레임워크 우선
- **Phase 0를 먼저 구현**
- 모든 변경사항을 Train/Test 양쪽에서 검증
- 과적합 여부 확인

### 하위 호환성
- 기존 코드가 그대로 작동해야 함
- 새 기능은 **옵션**으로 추가
- 기본값 유지 (레버리지 비활성화 등)

### 테스트 필수
- 각 단계마다 단위 테스트
- **Train/Test 양쪽에서 통합 테스트**
- 기존 결과와 비교

### 코드 품질
- 클린 코드 유지
- 주석 충분히
- 타입 힌트 사용
- Single Source of Truth (성과 지표는 `performance.py`만)

---

## 📚 참고 문서

- **Part 1**: 현황 분석 및 비판적 검토 (`BACKTEST_ENHANCEMENT_PART1_ANALYSIS.md`)
- **Part 2**: 상세 설계 및 구현 계획 (`BACKTEST_ENHANCEMENT_PART2_DESIGN.md`)
- **Summary**: 본 문서 (`BACKTEST_ENHANCEMENT_SUMMARY.md`)

---

## 🎯 다음 단계

1. **설계 문서 업데이트 완료** ✅
2. **내일부터 구현 시작**:
   - Phase 0: 간단한 Train/Test 분리 (0.5일)
   - Phase 1: 거래비용 모델 (상품별) (1~2일)
   - Phase 2: 레짐 노출 스케일링 (1~2일)
   - Phase 3: Train/Val/Test 고도화 (2~3일)
3. **단계별 검증**: Train/Test 양쪽에서 확인
4. **최종 통합 테스트**: 전체 시스템 검증
5. **문서화**: 구현 완료 후 사용 가이드 작성

---

**작성자**: Cascade AI  
**검토 필요**: 사용자 승인 후 구현 시작
