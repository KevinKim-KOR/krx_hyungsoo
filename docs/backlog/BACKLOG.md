# BACKLOG

POC 1단계부터 누적된 의도적으로 미룬 항목.

각 항목은 다음 4필드 형식을 따른다.

> **주의 (2026-08-24)**: 아래는 **서식 예시이며 실제 항목이 아니다.** 이전에는 예시가 본문 형식 그대로여서 항목 카운트에 함께 잡혔다. 코드블록으로 감싸 카운트에서 분리한다.

```text
  - **항목**: 한 줄 요약
  - **보류 사유**: 왜 지금 하지 않는가
  - **보류된 위험**: 미루면 발생 가능한 부작용
  - **재검토 트리거**: 언제 다시 꺼낼 것인가
```

전수 감사 결과 (완료 / 폐기 / 중복 / 현재 결함 escalate 분류 기록) 는 본 문서 외부 — `docs/handoff/POC2/POC2_BACKLOG_AUDIT_CONCLUSION.md` 에 보존된다.

---

## 최신 귀속·분류 기준 (2026-08-01 · POC3-REF-02 Canonical Alignment)

**본 canonical BACKLOG 본문(아래 §1~)에는 조건부 보류 65개만 유지**한다. 전체 105개 분류·귀속 이력은 통합지도 V2 §6과 Git 이력에서 보존한다.

전체 항목의 최신 상태와 담당 Lane 은 다음 문서를 **단일 기준**으로 한다.

`docs/ai_design/POC3/POC3-00_PC_JUDGMENT_UI_INTEGRATED_IMPLEMENTATION_MAP_V2.md`

5분류 (통합지도 V2 §6 기준 · 총 105개):

| 분류 | 개수 | canonical BACKLOG 본문 |
|---|---|---|
| 완료 | 17 | 본문에서 제거 (구현·검증·최신 결정으로 해소 · V2 §6 보존) |
| 확정 개발 | 3 | 본문에서 제거 · 통합지도 Lane 관리 (B-003→POC3-05 · B-072→POC3-04 · B-105→POC3-07) |
| 후속 개발 | 16 | 본문에서 제거 · POC3 이후/선행조건 이후 (POC4/POC5 등) |
| **조건부 보류** | **65** | **본문 유지 — 재검토 트리거 충족 전 미착수. 실질 관리 대상** |
| 제외·폐기 | 4 | 본문에서 제거 (개발하지 않음 · V2 §6 보존) |
| 합계 | **105** | 본문 유지 = 65 · 제거 = 40 |

- **조건부 보류 65개만** 본문에 4필드(항목·보류 사유·보류된 위험·재검토 트리거) 원문으로 유지한다(설계자 확정 2026-08-01 · POC3-REF-02 §4.4).
- 완료·확정 개발·후속 개발·제외·폐기 40개는 본문에서 제거했다. 이들의 분류·귀속·이력은 통합지도 V2 §6과 Git 이력에서 보존된다(별도 보관 문서 없음).
- 항목별 B-ID(B-001~B-105)는 통합지도 V2 §6 의 원본 BACKLOG(105개) 출현 순서 부여값이다. 본문에서 40개를 제거해 현재 65개만 남았으므로, V2 §6 과 대조할 때는 B-ID 로 매칭한다.
- 섹션 번호(§1~§16 등)는 원문 번호를 보존한다(항목이 모두 제거된 섹션은 헤더째 삭제되어 번호가 건너뛸 수 있음). 재번호하지 않는다 — V2 §6 이력 대조 유지 목적.
- 장기 순서는 예상이며, **다음 실제 Step 하나만 `docs/STATE_LATEST.md` 에서 확정**한다.
- 분류와 재검토 트리거 내용은 본 반영에서 **변경하지 않았다**(65개 원문 보존 · 40개 제거만 수행).

---

## 반영 이력 (2026-08-24 · BACKLOG 정합 R2.1)

`docs/handoff/BACKLOG_RECONCILIATION_V2_CONCLUSION.md`(검증자 VERIFIED · 커밋 `a22ddae5`)
결과를 본 원장에 기계적으로 반영했다. **67건 → 55건.**

| 처리 | 건수 | 항목 |
|---|---|---|
| **제거 — 완료** | 4 | 종목명 자동 조회(ETF 완료 · 개별주 수동 입력 확정) · 구성종목 fetcher timeout(`NAVER_TIMEOUT_SEC=10`) · 복수 포트폴리오·계좌(본인 다계좌 운영 중 — 실측 32건 / 3계좌) · 샘플 입력 폼 제거(개발·실험용 격리) |
| **제거 — 중복** | 1 | 종목명 자동 보정 → 종목명 자동 조회에 병합 |
| **제거 — 폐기** | 5 | 계좌별 세금 · 인증/사용자 구분 · Slack·Email·모바일 PUSH · Cboe VIX 수동 보정 · PC package fallback |
| **결함으로 승격** | 2 | ML 초안 생성 → `DEF-ML-CHAIN` · FDR timeout → `DEF-FDR-TIMEOUT` |
| **소계 (원장 이탈)** | **12** | |
| **남은 항목** | **55** | |

**범위 축소·정정 7건** (분류는 `VALID_FUTURE` 유지):

- Naver 동시성 → **시세 조회 경로 한정** (구성종목 경로는 방어 완료)
- ETF Category 라벨 → **근거 정정**: 컬럼은 있다(1,171행 전부 값 보유). 없는 것은 코드→라벨 매핑
- 운영 필드 확장 → **잔여 3필드** (`approved_at` 은 이미 구현됨)
- runtime source 확장 → **VIX 제외**
- 무릎/머리/어깨 · 보유↔외부 가중치 · RS/거래량/정배열 → **`Layer B`/`Layer C` 라벨 제거**

**구조 변경 3건**: 항목이 모두 빠진 **§1 헤더 삭제**(기존 정책) · **§14 섹션명 정정** ·
**서식 예시를 코드블록+들여쓰기로 분리**(이전에는 `grep -c` 가 예시까지 세어 실제보다 1 크게
나왔다 — 이제 `grep -c "^- \*\*항목\*\*"` **= 실제 항목 수**).
**섹션 번호는 재번호하지 않았다**(통합지도 V2 §6 `B-ID` 대조 유지).

**PC package fallback 처리 경계**: 폐기 대상은 **`state/three_push/packages/` 생성
파이프라인을 정식 자동 발송 경로로 되살리는 과제**다. 정식 경로는
`PARAM_HANDOFF_OCI_RUNTIME_3PUSH` 로 전환됐다. **기존 sync 산출물
(`scripts/sync_three_push_packages.py` · `scripts/run_three_push_sync_task.ps1`)은
보존**되며 manual recovery · smoke test · OCI 파일 전달 검증 · 비상 fallback 용도로 계속
쓴다(`docs/handoff/PC_THREE_PUSH_SYNC_TASKSCHEDULER.md` §0.2 · `docs/STATE_LATEST.md:1793`).

**`UNKNOWN` 0건** — 대기 중인 실측 없음.

---

## 현재 결함 (원장 밖 관리 · 2026-08-24 승격)

**BACKLOG 는 "지금 안 하는 게 맞는 미래 과제" 를 담는다. 아래는 그것이 아니라
"지금 제공하기로 되어 있는데 동작하지 않는 것" 이라 원장에서 분리한다.**
근거: `docs/handoff/BACKLOG_RECONCILIATION_V2_CONCLUSION.md` §4 (검증자 VERIFIED · 커밋 `a22ddae5`).

**현재 열린 결함: 2건** (`DEF-FDR-TIMEOUT` · `DEF-COMPUTE-TOPN-ASOF-HISTORICAL`).
`DEF-PRICE-EQUITY-REFRESH` 는 **2026-09-06 오등재로 철회**됐다 (운영은 정상).
`DEF-ML-CHAIN` 은 **2026-08-25 해소**됐다.

### ~~DEF-ML-CHAIN — ML feature/evidence 생성 체인 미작동~~ → **해소 (2026-08-25)**

**Step**: `POC3-ML-01` · **검증자 `VERIFIED`** · 완료조건 11개 전부 충족.
근거 — `docs/ai_result/POC3/POC3-ML-01_ML_FEATURE_AND_EVIDENCE_CHAIN_RESTORATION_RESULT.md`
(DESIGN `ai_design/POC3/POC3-ML-01_..._DESIGN_V1.md` · PLAN `ai_plan/POC3/POC3-ML-01_..._PLAN_V1.md`).

| 항목 | 해소 전 | 해소 후 |
|---|---|---|
| `etf_ml_feature_daily` | 0행 | **1,376,524행** (`2014-05-12`~`2026-08-20` · ticker 1,171) |
| `market_risk_feature_daily` | 0행 | **3,013행** |
| `relative_upside_score_latest.json` | 부재 | **1,157종목** (`asof 2026-08-20`) |
| `GET /market/topn/latest` | `status="unavailable"` · 3필드 전부 `null` | **`status="ok"`** · 참고점수·사유·`drawdown_20d` 실제 값 |
| ML job | `failed` | **3단계 success** |
| sanity | `error` (errors 3) | **`warn` · errors 0** |
| readiness | 축 전부 `empty` | **7축 전부 `available`** |
| 화면 | 열은 있는데 값 없음 · 정렬 무의미 | **참고점수·사유·고점 대비 표시 + 고점대비 정렬 실동작** (사용자 실화면 확인 완료) |

**원인**: 코드 결함이 아니라 **live DB 대상으로 실행된 적이 없었다.** 원천 가격은 이미
있었고(`etf_daily_price` 1,379,939행) 없던 것은 그로부터 만드는 feature 뿐이었다.

**코드 변경은 1건** — `ml_job_runner._run_feature` 의 0건 가드를 snapshot 기록 **앞으로**
옮기고 조건을 `and` → `or` 로 바꿨다(두 산출물 모두 필수). 회귀 테스트 4건 추가.

**계산 불가 종목은 `0` 으로 위장하지 않는다** — lookback 미충족 13종목은 점수에서 제외되고
화면에 `—` 로 표시된다. warm-up 구간 `return_20d IS NULL` 20,190행 유지.

**증분 갱신은 수동**(설계 확정 D-3) — `ML 실험` → `ML 근거 갱신` 또는
`POST /ml/jobs/evidence-refresh`. 자동 스케줄러·OCI 역할 변경 없음.

---

### DEF-COMPUTE-TOPN-ASOF-HISTORICAL — `compute_topn(asof=과거일)` 이 과거를 재현하지 않음

**등재**: 2026-08-29 · 설계자 승인 (POC3-ML-02 판정문). **발견 Step**: `POC3-ML-02`.

- 상태: BACKLOG
- 발견 Step: POC3-ML-02
- 문제:
  `compute_topn(asof=과거일)` 호출 시 기준일만 과거로 이동하고 최신가격은
  `history[-1]`을 사용하므로 미래 가격이 섞인 수익률을 반환한다.
- 현재 운영 영향:
  없음. 확인된 운영 호출자 7곳은 모두 `asof`를 전달하지 않고 DB 최신일을 사용한다.
- 보류 사유:
  현재 운영 경로에는 영향이 없으며, 이번 Step에서 운영 함수까지 수정하면 범위가 확대된다.
- 보류된 위험:
  향후 백테스트·과거 화면·과거 API가 `asof`를 사용하면 미래 데이터가 섞인 결과를
  정상값으로 오인할 수 있다.
- 재검토 트리거:
  `compute_topn(asof=...)`를 운영 외 과거 재현·백테스트·API에서 사용하기 전.
- 현재 우회:
  POC3-ML-02 평가에서는 기준일 이후 데이터를 제거한 임시 SQLite를 사용해
  기존 `compute_topn`을 변경 없이 호출한다.

**실측 근거** — `app/market_topn_helpers.py:140 _compute_period()` 가 최신가를
`history[-1]` 에서 가져오고, `app/market_topn.py:207` 이 `fetch_price_history(tk)` 를
날짜 제한 없이 호출한다. `compute_topn(n=10, asof="2016-06-30")` 이 1개월 수익률
**1017.26%** 를 반환했고(해당 종목 실제 종가는 연속적), 절단 임시 DB 로 재현하면
**5.38%** 다. 상세: `docs/ai_result/POC3/POC3-ML-02_..._RESULT.md` §10.

### ~~DEF-PRICE-EQUITY-REFRESH — 보유 개별주의 일별 가격이 갱신되지 않음~~ → **오등재 · 철회 (2026-09-06)**

**등재**: 2026-09-05 · **철회**: 2026-09-06 (개발자 오등재). **발견 Step**: `POC3-OPS-01A`.

- 상태: **CLOSED — 결함 아님**
- 원래 주장:
  보유 원장에 개별주가 있는데 일별 가격 갱신 대상이 `etf_master` 로 제한돼
  개별주 가격 이력이 단절된다. 유예 약 4 거래일.
- **실측 결과 — 운영에는 결함이 없다 (2026-09-06, OCI 읽기 전용)**:

  | 확인 | 값 |
  |---|---|
  | OCI 배치 최근 실행 | `attempted=41 success=41 fail=0` · `status=success` |
  | OCI DB 개별주 3종 마지막 종가 | **2026-09-03** (정상 갱신 중) |
  | 로컬 DB 개별주 3종 마지막 종가 | 2026-08-12 (정체) |

- 오등재 원인:
  **로컬 개발 DB만 보고 운영 결함으로 단정했다.** 두 경로는 갱신 대상이 다르다.

  | 경로 | 대상 | 개별주 포함 |
  |---|---|---|
  | OCI 배치 `run_oci_market_data_batch.py` | `collect_approved_tickers()` = **승인 seed ∪ 현재 Holdings** (41개) | **포함** |
  | 로컬 `POST /market/refresh` | `fdr.StockListing("ETF/KR")` → `etf_master` (약 1,178개) | 미포함 |

  `collect_approved_tickers()` 는 처음부터 Holdings 를 합친다
  (`app/three_push_runtime/market_data_batch.py:85`). 개별주가 갱신 대상에서
  빠진 적이 없다.

- 교훈:
  **로컬 DB 상태를 운영 상태로 단정하지 말 것.** 개발 환경은 ETF 유니버스만
  갱신하므로 Holdings 전용 ticker(개별주)가 정체된다. 운영 판단이 필요하면
  OCI DB·배치 로그를 직접 확인한다.

> 아래는 남아 있는 **개발 환경** 한계다. 결함으로 등재하지 않는다.
> 로컬에서 보유 개별주를 포함해 분석하려면 `collect_approved_tickers()` 경로를
> 쓰거나 해당 ticker 를 수동 적재해야 한다.

### DEF-FDR-TIMEOUT — FDR 호출 timeout 부재

- **원장 출처**: 구 §3 `FDR 외부 의존 약관 / 안정성 / 단일 호출 timeout 부재` (B-014)
- **현재 동작**: `app/market_data_fdr.py` 에 `timeout` **0건**. 호출자 3곳
  (`market_refresh_service.py:33` · `market_benchmark_store.py:189` ·
  `three_push_runtime/market_data_batch.py:147`) **어디에도 상위 timeout·budget 없음**.
- **필수 운영 경로 여부**: **그렇다.** `market_refresh_log` 실측 — `FinanceDataReader/prices`
  가 2026-08-20 **1,171종목 · 140.4초** 실행(성공 1171 / 실패 0).
- **사용자 영향**: hang 관측 이력은 없으나 **1,171종목 순차 호출이라 한 종목이 멈추면
  시장 갱신 전체가 멈춘다.** 방어 수단이 어느 계층에도 없다.

---

### 승격 시점 원문 (참고 보존)

<details>
<summary>원장에서 옮겨온 4필드 원문</summary>

```
  - **항목**: ML 기반 초안 생성 / 분석 연결 (추천 로직 / 정렬 / score 도입, 추천 판단 사유 message 노출)
  - **보류 사유**: ML 단계 진입 전 단순 rule-based 운영 유지. ML 산출물 안정화 전 메시지 노출 시 사용자 판단 흐림.
  - **보류된 위험**: 사용자가 message 만 보고 매매 신호로 오해할 수 있음.
  - **재검토 트리거**: 상대상승 축1 baseline 후속으로 factor·threshold 가 1차 확정되거나, ML 결과 신뢰도가 정성적으로 합의될 때.

  ────────────────────────────────

  - **항목**: FDR 외부 의존 약관 / 안정성 / 단일 호출 timeout 부재
  - **보류 사유**: FDR 비공식 출처. 본 단계에서 약관 / 호출 빈도 / per-ticker timeout 미검토.
  - **보류된 위험**: 출처 변경 / 차단 시 운영 단절. FDR hang 시 장시간 차단 가능.
  - **재검토 트리거**: FDR 호출 운영 중 1회 실패 또는 4분+ hang 케이스 발생 시.

---
```

</details>

---

## 2. 위험 evidence / 시계열 / 데이터 품질

- **항목**: 급락 신호 UI 고도화
- **보류 사유**: 현재 message_text + UniverseRefreshPanel ⚠ 1줄만 노출. 가격 흐름 evidence 미제공.
- **보류된 위험**: Telegram 만으로 급락 근거 식별 어려움.
- **재검토 트리거**: 사용자가 급락 후보의 가격 흐름 UI 명시 요청 시.

---

- **항목**: 급락 후보(`falling_candidate`)의 안정적 latest 읽기 GET 계약 (POC3-05 §13.1)
- **보류 사유**: 자동 조회 가능한 안정적 GET 계약 부재. 신규 API·저장 계약으로 범위 확대되어 POC3-05 확인 근거 화면에서 제외.
- **보류된 위험**: 보유 ETF 와 Universe 급락 후보의 일치 여부를 확인 근거 화면에서 자동 확인 불가.
- **재검토 트리거**: latest 저장 계약이 마련되고 실제 운영에서 일치 확인 필요성이 반복 입증될 때.

---

- **항목**: 보유 ETF 종합 위험 구간 분류 (factor·threshold·label) (POC3-05 §13.2)
- **보류 사유**: 운영·Outcome 근거 없이 위험 구간(저위험/고위험 등)을 정의할 수 없음. POC3-05 는 표시·정렬만 하고 위험 판정·저장 rank 를 만들지 않는다.
- **보류된 위험**: 확인 순서는 줄여주지만 위험 수준을 정량 비교하지 못함.
- **재검토 트리거**: First Real Decision Cycle 과 Decision Outcome Ledger 에 실제 사례가 누적될 때.

---

- **항목**: 구성종목 등락률 (가격 시계열 source 진단 · 개별주 시세 수집)
- **보류 사유**: **2026-08-19 설계 확정으로 이번 Step 에서 하지 않기로 결정.** 개별주 시세의 **조회 시점·기준일·중복 제거·저장 위치·실패 처리까지 새로운 데이터 계약**이 필요하다. 기존 네이버 개별주 조회 경로(`app/market_naver.py` — 보유 종목 시세용)가 있어도 수십~수백 종목을 새로 조회하는 것은 단순 재사용이 아니라 **신규 수집 계약**이며 `KILL_SWITCHES §5 "실시간 외부 API 의존 강화"` 방향이다. 별도 Step 판단 사안. (이전 사유: source FDR / pykrx / KRX Open 미연동.)
- **보류된 위험**: 구성종목 중 당일 상승·하락 종목과 **ETF 움직임의 단기 원인**을 화면에서 확인할 수 없다.
- **재검토 트리거**: 구성종목별 당일 움직임이나 `비중 × 등락률` 기여도가 실제 투자 판단에 필요하다고 **사용자가 확인**했을 때.
- **그동안의 처리 (2026-08-19)**: 정상 화면에서 **등락률 열과 `등락률 unavailable` 안내문을 제거**했다. `0%`·ETF 자체 등락률·추정값으로 대체하지 않으며, API·DB 에 빈 등락률 필드를 만들지 않는다.

---

## 3. NAV / 시장 데이터 source

- **항목**: Naver Mobile stock integration API 운영 안정성 추가 진단
- **보류 사유**: NAV / Discount Source Diagnosis 1차 4 sample ticker NAV+시장가격 OK. 비공식 endpoint 라 운영 안정성 미검증.
- **보류된 위험**: schema 변경 / 차단 / 429 발생 시 운영 단절.
- **재검토 트리거**: 사용자가 NAV / 괴리율을 운영 판단에 사용 결정 시.

---

- **항목**: NAV source + FDR 시장가격 결합 패턴
- **보류 사유**: Naver 안정성 미검증 상태. NAV 만 Naver, 시장가격은 FDR 결합 시 호출 부담 경감 가능.
- **보류된 위험**: 단일 source 채택 후 결합 패턴 재설계 비용.
- **재검토 트리거**: Naver Mobile NAV Source Stability 진단 결과 adopt 승격 시.

---

- **항목**: pykrx ETF endpoint 자체 동작 진단
- **보류 사유**: pykrx 1.0.51 에서 ETF NAV / 가격편차 함수가 empty 응답. 본 단계에서 깊이 분석 X.
- **보류된 위험**: pykrx fallback 경로 부재.
- **재검토 트리거**: pykrx upstream 업데이트 또는 KRX 데이터 정책 변경 시.

---

- **항목**: Naver endpoint 변경 / 차단 대응
- **보류 사유**: 안정 운영 중. source 분리 / fallback 로직 미구현.
- **보류된 위험**: 동일 reason 으로 일괄 실패 시 운영 단절.
- **재검토 트리거**: Naver 차단 실측 발생 시.

---

- **항목**: Naver fetch 동시성 / 배치 / rate limit — **시세 조회 경로 한정**
- **범위 축소 (2026-08-24)**: 구성종목 수집 경로는 방어 완료(`etf_constituents_service.py` — ticker당 0.5s delay · 30s budget · 1회 10개 상한). **남은 것은 시세 조회 경로**(`app/market_naver.py` — 종목별 순차 호출, 배치·동시성 없음).
- **보류 사유**: 보유 종목 수 적음. 직렬 호출로 충분.
- **보류된 위험**: 종목 수 증가 시 latency 누적.
- **재검토 트리거**: 보유 종목 수 50+ 또는 latency 보고 시.

---

- **항목**: pykrx EOD fallback 추가 (POC2-Step2A)
- **보류 사유**: Naver 단일 source 충분. fallback 미구성.
- **보류된 위험**: Naver 차단 시 데이터 단절.
- **재검토 트리거**: Naver 차단 트리거 발생 시.

---

- **항목**: market_cache TTL / 만료 정책
- **보류 사유**: 단일 환경 1일 운영. TTL 정책 미수립.
- **보류된 위험**: stale 데이터 노출.
- **재검토 트리거**: stale 노출 사용자 보고 시.

---

- **항목**: Naver 응답 캐싱 헤더 / ETag 활용
- **보류 사유**: 호출량 적음.
- **보류된 위험**: 호출량 증가 시 비효율.
- **재검토 트리거**: 호출량 임계 초과 시.

---

- **항목**: market_cache 복수 source 우선순위 / 메타데이터
- **보류 사유**: 단일 source 운영. 우선순위 정책 불필요.
- **보류된 위험**: 다중 source 도입 시 우선순위 결정 비용.
- **재검토 트리거**: Step2A (pykrx fallback) 진입 시.

---

- **항목**: GET /holdings/enriched 응답 캐싱 / 조건부 응답
- **보류 사유**: 보유 종목 수 적음. 캐싱 불필요.
- **보류된 위험**: 100+ 종목 시 응답 latency.
- **재검토 트리거**: 보유 종목 100+ 시.

---

- **항목**: enrichment 단계 로깅 / 감사 추적
- **보류 사유**: 운영 중 enrichment 실패 추적 어려움.
- **보류된 위험**: Naver 차단 의심 시 원인 파악 비용.
- **재검토 트리거**: Naver 차단 의심 케이스 발생 시.

---

- **항목**: 가격 미확인 종목 반복 실패 관리
- **보류 사유**: 단발 실패는 [시세미확인] 표기로 처리.
- **보류된 위험**: 1주일+ 반복 실패 시 사용자 혼란.
- **재검토 트리거**: 동일 종목 7일+ 실패 발생 시.

---

- **항목**: 시세 표시 정밀도 / 통화 / 단위
- **보류 사유**: 원화 단일. 통화 / 단위 정책 단순.
- **보류된 위험**: 외화 종목 보유 시 단위 혼선.
- **재검토 트리거**: 외화 종목 보유 발생 시.

---

## 4. Market Discovery / Universe

- **항목**: Market Discovery 기본 조회 기준 설정 UI
- **보류 사유**: DEFAULT_BASIS=one_month 코드 상수 고정. Settings 화면 미도입.
- **보류된 위험**: 기본 기준 변경 마찰.
- **재검토 트리거**: 사용자가 1개월 기본 기준 부적합 보고 또는 Settings 화면 도입 시.

---

- **항목**: ETF Category 라벨 매핑
- **근거 정정 (2026-08-24)**: `etf_master` 에 **`category` 컬럼은 존재**한다(`app/market_data_store.py:25` `category TEXT`). DB 실측 **1,171행 전부 값 보유 · 고유값 `1`~`7`**. **미완료인 것은 컬럼이 아니라 코드 → 사용자 표시 라벨 매핑**이다.
- **보류 사유**: FDR Category 컬럼이 정수 코드. 라벨 매핑 미정의.
- **보류된 위험**: 섹터 / 테마 발굴 시 Category 활용 불가.
- **재검토 트리거**: TOP N 화면 구현 STEP 진입 시.

---

## 5. ETF 구성종목 / 중복률

- **항목**: ETF 구성종목 fetcher 다변화 + chain fallback
- **보류 사유**: Naver Stock ETFComponent 1차 채택 완료 (2026-05-31). 단일 fetcher.
- **보류된 위험**: Naver 실패 ETF 에 대해 대안 부재.
- **재검토 트리거**: Naver 실패 ETF 누적 또는 KRX Open API 인증 확보 시.

---

- **항목**: 중복률 임계 기반 경고
- **보류 사유**: 운영 후 임계 기준 미확정.
- **보류된 위험**: 후보 ETF 쌍이 사실상 동일 노출인 경우 사용자 인지 비용.
- **재검토 트리거**: weighted_overlap_pct 가 임계 초과하는 쌍이 반복 관측 시.

---

- **항목**: 구성종목 fuzzy 매칭
- **보류 사유**: 현재 ticker / reuters / ISIN / 정규화 name 매칭.
- **보류된 위험**: 한국어 표기 변형 미스매칭.
- **재검토 트리거**: 표기 변형 미스매칭 실측 시.

---

## 8. Holdings / 포트폴리오 구조

- **항목**: holdings 스키마 확장 (평균단가 외 필드 — 매수일자 / 메모 / 목표가)
- **보류 사유**: 현재 ticker + qty + 평균단가 + account_group 최소.
- **보류된 위험**: 매수일자 / 메모 / 목표가 등 필드 부재.
- **재검토 트리거**: 사용자 명시 요구 시.

---

- **항목**: account_group 라벨 병합 / 관리 UI
- **보류 사유**: 라벨 자유 입력. 병합 UI 미구현.
- **보류된 위험**: 라벨 파편화로 그룹 통계 왜곡.
- **재검토 트리거**: 라벨 파편화 실측 시.

---

- **항목**: 과거 draft_payload 영구 마이그레이션
- **보류 사유**: 현재 JSON SSOT 유지. 과거 schema 변경 시 마이그레이션 미수행.
- **보류된 위험**: 과거 run 재현 불가.
- **재검토 트리거**: 사용자가 과거 run 재현 요청 시.

---

- **항목**: stable holding_id 영구 저장
- **보류 사유**: 현재 ticker 기반 식별. 동일 ticker 다중 계좌 시 row 식별 충돌 가능.
- **보류된 위험**: 다중 계좌 시 row 식별 깨짐.
- **재검토 트리거**: 다중 계좌 운영 또는 동일 ticker 분할 관리 시.

---

## 9. Message / Telegram / 알림

- **항목**: Telegram 메시지 Top N 설정값 UI 화
- **보류 사유**: 현재 코드 상수.
- **보류된 위험**: 사용자가 Top N 변경 마찰.
- **재검토 트리거**: 사용자 요구 시.

---

- **항목**: 계좌별 Telegram 요약 / 채널 분리
- **보류 사유**: 단일 채널 운영. 와이프 / 본인 분리 미구현.
- **보류된 위험**: 와이프 / 본인 메시지 혼재.
- **재검토 트리거**: 와이프 공유 시점.

---

- **항목**: 시세 확인 종목 기준 요약 UX 고도화
- **보류 사유**: 현재 시세 확인 / 미확인 종목 혼재 표시.
- **보류된 위험**: 시세 미확인 종목 누적 시 사용자 혼란.
- **재검토 트리거**: 누적 혼란 보고 시.

---

- **항목**: 누락 데이터 많은 포트폴리오 별도 경고 정책
- **보류 사유**: 누락률 임계 정책 미수립.
- **보류된 위험**: 50%+ 미확인 시 PnL 신뢰도 저하.
- **재검토 트리거**: 50%+ 미확인 실측 시.

---

- **항목**: 시세 미확인 계좌 UX 고도화
- **보류 사유**: 7일+ 계산 불가 계좌 별도 표시 미구현.
- **보류된 위험**: 사용자가 미확인 원인 파악 비용.
- **재검토 트리거**: 7일+ 계산 불가 계좌 발생 시.

---

- **항목**: PnL 산식 설명 고도화
- **보류 사유**: 현재 단순 (현재가 - 평균단가) × 수량.
- **보류된 위험**: 사용자가 PnL 산식 의문 시 설명 부족.
- **재검토 트리거**: 사용자 질문 발생 시.

---

- **항목**: message_text 와 Telegram 실제 렌더 차이 자동 검증
- **보류 사유**: 현재 수동 검증.
- **보류된 위험**: 깨진 메시지가 사용자에게 노출.
- **재검토 트리거**: 깨짐 보고 또는 Telegram API 변경 시.

---

## 10. UI / Frontend

- **항목**: 근거 데이터 접힘 상태 영구 저장 (localStorage / URL Query)
- **보류 사유**: 현재 새로고침 시 초기화.
- **보류된 위험**: F5 시 사용자 컨텍스트 손실.
- **재검토 트리거**: 사용자 보고 시.

---

- **항목**: 상세 에러 UX 개선
- **보류 사유**: 현재 단순 에러 메시지.
- **보류된 위험**: 사용자가 에러 원인 파악 비용.
- **재검토 트리거**: 에러 분류 / 안내 요구 시.

---

- **항목**: 그리드 디자인 — **컬럼 프리셋 잔여분** (2026-08-02 사용자 지적분 중)
- **보류 사유**: 원 지적(안티그래비티 대비 완성도)은 **사용자 직접 지시 UI 작업으로 대부분 소진**됐다. 행 높이·가독성은 POC3-08(보유 현황·종목 관리 증권사 스타일)과 2026-08-12~16 카드 전환(ETF 비교하기 후보·보유, 확인 근거)으로, 정렬 화살표는 후보 탭 정렬 바로, 값 컬러 코딩은 손익 색 작업으로 해소. **다크 테마는 사용자 결정으로 드랍**(2026-08-16 — 앱이 라이트 단일 톤 확정, `globals.css :root` "토글 미지원"). 남은 것은 컬럼 프리셋 하나다.
- **보류된 위험**: 컬럼이 많은 표(구성종목 중복 11열·데이터 상태 9열·요즘 잘 오르는 ETF 17열 등)에서 필요한 열만 골라 보는 수단이 없다.
- **재검토 트리거**: 사용자가 특정 화면에서 "열이 너무 많다 / 이 열만 보고 싶다" 를 실제로 지적할 때. **유지·드랍 판단 보류 중**(2026-08-16 — 적용 범위·영향도 확인 후 결정 예정).

---

- **항목**: 수동 새로고침 시 run_id 유실 방지
- **보류 사유**: 현재 sessionStorage 기반.
- **보류된 위험**: 새로고침 시 run 컨텍스트 손실.
- **재검토 트리거**: 사용자 보고 시.

---

- **항목**: 모바일 최적화 (compact table / 터치 UX)
- **상태 (2026-07-22 재정정, Mobile Decision Cockpit v1 Deferred)**: **BACKLOG 유지 · DEFERRED_BY_USER**. 2026-07-20 승격 결정은 최신 사용자 결정으로 보류. 재개 시 첫 후보는 **Telegram Cockpit** (모바일 Web 아님).
- 원 보류 사유 (이력): 현재 PC 데스크톱 중심. PROJECT_ORIGIN_INTENT "모바일 UI 후순위" 원칙 유지.
- 원 보류된 위험 (이력): 와이프 / 친구 공유 시 모바일 가독성 저하.
- 재검토 트리거 (2026-07-22 정정): 다음 4개 모두 충족 시 · 사용자 명시 재개 결정 시에만.
  1. PC 판단 흐름 완성
  2. Telegram 저빈도 운영 안정화
  3. 모바일 부재가 다시 실제 운영 차단 사유
  4. 사용자 명시 재개 결정
- 상세: `docs/backlog/POC2_MOBILE_DECISION_COCKPIT_DEFERRED_CONCLUSION.md`.
- 이전 앵커 (SUPERSEDED): `docs/handoff/POC2/POC2_MOBILE_DECISION_OPERATING_SEQUENCE_ANCHOR.md`.

---

- **항목**: app/api.py 라우터 분리
- **보류 사유**: 2026-08-16 실측 **632 라인**(직전 기록 557). KS-10 트리거 (700+ 라인) 여전히 미달. 라우터는 이미 `app/api_*.py` 18개 파일로 분리돼 있고 본 항목은 잔여 `app/api.py` 를 더 쪼갤지의 문제다.
- **보류된 위험**: 추가 endpoint 누적 시 단일 파일 비대.
- **재검토 트리거**: 700 라인 초과 시 또는 신규 endpoint 추가 작업 시.

---

## 11. OCI / Delivery / Operations

- **항목**: 실패 상태 세분화 (DRAFT_FAILED / PUSH_FAILED / ALERT_FAILED)
- **보류 사유**: 현재 단일 FAILED 상태. 복구 액션 분기 미구현.
- **보류된 위험**: 실패 원인별 복구 액션 결정 비용.
- **재검토 트리거**: 실패 유형 다양화 실측 시.

---

- **항목**: 부분 재시도 정책 (Retry Push / Retry Alert)
- **보류 사유**: 현재 전체 재실행만.
- **보류된 위험**: 외부 실패 1회로 전체 재실행 비효율.
- **재검토 트리거**: 외부 실패 빈도 증가 시.

---

- **항목**: 운영 필드 확장 — **잔여 3필드**(rejected_at / error_code / error_message)
- **범위 축소 (2026-08-24)**: `approved_at` 은 **이미 구현됨**(`app/delivery.py:128` · `app/runtime_state_db.py:39`). 나머지 3필드만 남는다.
- **보류 사유**: 현재 필드 최소.
- **보류된 위험**: SLA 지표 / 운영 추적 부재.
- **재검토 트리거**: 운영 SLA 지표 요구 시.

---

- **항목**: DELIVERING polling / timeout / orphan 처리 고도화
- **보류 사유**: 현재 POC 운영. 고도화 미수행.
- **보류된 위험**: orphan run 누적 또는 polling 비효율.
- **재검토 트리거**: POC 운영 안정화 후 또는 orphan 실측 시.

---

- **항목**: 알림 재시도 정책 (5xx / 429)
- **보류 사유**: 현재 단발 발송.
- **보류된 위험**: Telegram API 일시 장애 시 알림 손실.
- **재검토 트리거**: 5xx / 429 빈도 증가 시.

---

- **항목**: OCI 측 처리 timeout / orphan 대응
- **보류 사유**: inbox 적체 처리 미구현.
- **보류된 위험**: inbox 적체 발생 시 운영 단절.
- **재검토 트리거**: inbox 적체 실측 시.

---

- **항목**: runtime data source 확장 (CNN F&G / VIX / USD KRW / 원유 / news / holdings valuation)
- **범위 축소 (2026-08-24)**: **VIX 는 이미 구현됨**(`app/api_market_topn_models.py:228` · `app/api_decision_draft_preview.py:42`). 잔여 source 는 CNN F&G · USD/KRW · 원유 · news · holdings valuation.
- **보류 사유**: 현재 OCI runtime 빌더에서 모두 unavailable 기본 표시.
- **보류된 위험**: PUSH 메시지의 운영 컨텍스트 가치 저하.
- **재검토 트리거**: 사용자가 source 별 가용성 명시 요청 또는 OCI read model foundation 진입 시.

---

## 13. Universe / Cache 후순위

- **항목**: pykrx 외 fallback 데이터 source
- **보류 사유**: pykrx 단일.
- **보류된 위험**: KS-9 외부 의존 위험.
- **재검토 트리거**: pykrx 차단 또는 KRX Open API 인증 확보 시.

---

- **항목**: 2014-04-07 이전 ETF 시계열 보강
- **보류 사유**: 현재 개인 프로젝트의 첫 ML·위험 evidence 기반은 2014-04-07 이후 실제 접근 가능한 데이터로 시작한다. 시장 시계열 Closeout STEP (2026-06-30) 에서 네이버/FDR 이 반환하는 KODEX200 최초 관측일이 2014-04-09 로 실측 확인됨.
- **보류된 위험**: 장기 백테스트 기간이 제한될 수 있음.
- **재검토 트리거**: 2014년 이전 구간이 모델 성능 또는 백테스트 범위에 실질적으로 필요하다는 근거가 확인될 때.

---

## 14. 판정 보류 지표 (ASSUMPTIONS `Layer A` 연계)

> **섹션명 정정 (2026-08-24)**: 이전 이름은 `Layer 활성 관리 (ASSUMPTIONS 연계)` 였으나, `ASSUMPTIONS.md` 에 정의된 것은 **`Layer A` 하나뿐**이다(`:72` — "Layer A 관리 항목"). `Layer B`·`Layer C` 는 정의 문서가 없어 아래 항목 제목에서도 제거했다. **섹션 번호 14 는 보존**한다(통합지도 V2 §6 `B-ID` 대조 유지).

- **항목**: 무릎 / 머리 / 어깨 정량 기준
- **라벨 제거 (2026-08-24)**: `Layer B` 는 정의 문서가 없어(정의된 것은 `ASSUMPTIONS.md` 의 `Layer A` 뿐) 제목에서 제거. 항목 정의는 `docs/handoff/POC2/POC2_STEP7_SYSTEM_OUTPUT_3_PUSH_REALIGNMENT_DESIGN.md` **§9.4** · 완료 기준 **AC-10**.
- **보류 사유**: 보유 상태 브리핑 운영 후 정량 기준 미확정.
- **보류된 위험**: 점검값이 실제 투자 판단 품질 설명 부족.
- **재검토 트리거**: 보유 상태 브리핑 운영 후 사용자가 점검값 부족 명시 시.

---

- **항목**: 보유 점검과 외부 발굴 가중치
- **라벨 제거 (2026-08-24)**: `Layer B` 라벨 제거(위와 동일 사유). 근거 `POC2_STEP4_MOMENTUM_ENGINE_DIRECTION_AND_Q4_BOUNDARY_DESIGN.md:73`.
- **보류 사유**: 3-PUSH 운영 충돌 정책 미수립.
- **보류된 위험**: PUSH 1 / 2 가중치 결정 비용.
- **재검토 트리거**: 3-PUSH 운영 충돌 실측 시.

---

- **항목**: RS / 거래량 / 정배열 복합 지표
- **라벨 제거 (2026-08-24)**: `Layer C` 는 정의 문서가 없어 제목에서 제거. 항목 정의는 `POC2_STEP7...DESIGN.md` **§9.6** · 완료 기준 **AC-12**.
- **보류 사유**: ML 단계 보류.
- **보류된 위험**: ML 단계 진입 시 복합 지표 기준값 결정 비용.
- **재검토 트리거**: ML 단계 진입 시.

---

## 16. 메타 / 검증 항목

- **항목**: manual seed 입력 UX 개선 (seed 편집 UI)
- **보류 사유**: starter seed 부분 도입 완료. 후보군 변경 시 여전히 파일 직접 수정 필요.
- **보류된 위험**: 후보군 변경 부담이 운영 사이클 지속성 저하.
- **재검토 트리거**: 사용자가 운영 중 후보군 직접 수정 필요 상황 1회+ 보고 또는 PUSH 2 두 번째 구현 라운드 진입 시.

---

- **항목**: 와이프 UI 이해도 검증
- **상태 (2026-07-22 재정정, Mobile Decision Cockpit v1 Deferred)**: **BACKLOG 유지 · DEFERRED_BY_USER**. 2026-07-20 AC 승격은 Mobile Deferred 로 함께 보류.
- 원 보류 사유 (이력): 장기 성공 기준. 현재 모바일 UI 후순위 원칙 유지.
- 원 보류된 위험 (이력): 장기 성공 기준 검증 지연.
- 재검토 트리거 (2026-07-22 정정): Mobile Cockpit 재개 시 (모바일 최적화 항목과 동일 트리거).
- 상세: `docs/backlog/POC2_MOBILE_DECISION_COCKPIT_DEFERRED_CONCLUSION.md`.

---

## Universe Seed Bootstrap 후속 (Universe Momentum Evidence Publication v1 이월)

- **항목**: 보유/외부 후보 비율 가변화 (현재는 10/10 고정)
- **보류 사유**: 첫 승인 세션에서는 고정 비율로 단순성 확보. 운영 데이터 축적 후 재검토.
- **보류된 위험**: 사용자 포트폴리오 변화 시 후보 비율이 실제 관심도와 어긋날 수 있음.
- **재검토 트리거**: Telegram Contentful Controlled Send v1 완료 후 1회전 운영 데이터 확보 시.

---

- **항목**: ML·백테스트 기반 seed 품질 개선
- **보류 사유**: 현재 정성 판단 기반. ML 산출물 신뢰도 정성 합의 전 seed 자동화 금지.
- **보류된 위험**: seed 품질이 사용자 판단에만 의존하여 감사 어려움.
- **재검토 트리거**: ML baseline v1 안정화 후.

---

