# PROJECT ORIGIN INTENT

작성자: 사용자 본인
작성일: 2026-04-21
검토 주기: 분기 1회
성격: 불변 앵커

---

## 1. 한 줄 정의

**AI와 함께 투자 방향 찾기**

---

## 2. 1년 뒤 실제로 하고 싶은 일

현실적 목표:
- 잘 올라가는 섹터를 발굴하여 AI와 대화/판단
- 상황에 맞는 factor를 붙여가며 ML을 돌림
- 투자 초보자 단계에서 벗어나 나에게 맞는 투자 발견

최대 목표 (허황된 꿈으로 라벨):
- 상황별 factor 취사선택 (탐욕지수, 섹터 가격, 외부 이벤트 등)
- 다양한 factor 기반 ML 판단 시스템

---

## 3. 시스템 출력 정의

### 3.1. 출력 정의 상태 (2026-05-14 기준)

본 항목의 정식 출력 정의는 **B 방향 전환 직후 단계에서는 확정하지 않는다**.
다음 단계의 PC 작업 1~2단계 가능성 확인이 끝난 뒤 확정한다.

확인 범위:
1. pykrx 로 한국 상장 ETF 전체 universe 조회 가능 여부.
2. ETF 별 일간 / 1개월 / 3개월 수익률 계산 가능 여부.
3. 수익률 기준 TOP N 산출 가능 여부.

TOP N 의 N 값은 고정하지 않고 운영/테스트 중 변경 가능하게 둔다.

PC 작업의 핵심 방향 후보 (출력 정의 확정 전 단계):
- 섹터/테마 발굴 후보 산출 (한국 상장 ETF universe 기반).
- AI 투자세션에서 사용자가 직접 해석할 수 있는 정량 재료 제공.
- ML 연결 여부는 위 1~3 가능성 확인 결과에 따라 별도 판단.

### 3.2. 보조 출력 — 기존 Telegram PUSH 배관

기존 3-PUSH 구조는 **메인 출력이 아니라 보조 알림 배관**으로 유지한다.
이 분류는 Step7 시리즈에서 운영화된 배관을 **삭제하지 않고 보존**하기 위함이다.

- 보유 종목 상태 브리핑 (PUSH 1) — 상태 브리핑 재료, 매수/매도 의견 아님.
- 신규 ETF 관찰 후보 (PUSH 2) — 관찰 후보 알림, 진입 추천 아님.
- 급락 ETF 주의 신호 (PUSH 3) — 단순 임계값 기반 주의 신호.

위 3-PUSH 는:
- B 방향 전환 이후에도 **있는 그대로 유지**한다.
- 메인 출력이 정의되기 전까지는 **운영 의사결정의 1차 입력으로 사용하지 않는다**.
- 진입 시점 정량 분석은 시스템이 하지 않으며, 진입 여부 판단은 사용자가 AI
  투자세션에서 토론 후 결정한다.

## 시스템 책임 경계

- 시스템: 시장 흐름 판별, 정량 기준 적용, 알림 생성
- 사용자: 알림 해석, AI 투자세션 토론, 최종 매매 결정
- 매수/매도 API 자동화 없음
- "이상 감지" 자체는 사용자가 알림 보고 판단

---

## 4. 성공 기준 3개 (외부 검증형)

1. **친구의 긍정적인 놀람** — 친구에게 보여줄 때 "이거 대단하다"는 반응
2. **4070s의 동작** — ML 이외 작업을 못할 정도로 GPU가 돌아가는 상태
3. **와이프가 이해할 수 있는 UI** — 전문 용어 없이 읽히는 화면

**검증 우선순위 메모 (2026-05-11)**:
- 현재 검증 우선순위: 1차 검증자는 사용자 본인이다.
- 와이프 UI 이해도 검증은 사용자 본인이 운영 정착을 확인한 뒤 BACKLOG 에서 복귀한다 (docs/backlog/BACKLOG.md "와이프 UI 이해도 검증" 항목).
- 이 항목은 장기 성공 기준으로 유지한다 — 본 §4 의 3번 항목은 삭제하지 않는다.

---

## 5. 실패 기준

- **감지 기준**: 추천한 ETF 전체 평균이 마이너스 수익률
- **성과 목표**: 같은 기간 KODEX 200 대비 초과 수익

MDD 10% 같은 숫자 목표는 2차 기준. 1차는 위 두 가지.

---

## 6. 친구와 다르게 가고 싶은 부분

- **ML 엔진 검증 가능성**: 친구는 본인 판단 중심, 나는 AI+ML 교차 검증
- **합리적 판단 방식**: 여러 사람 조언 + AI 연구를 통한 판단
- **AI를 검증할 ML 보유**: AI에 맡기지 않고 검증 가능한 구조

단, ML 성능이 부족하면 결국 친구와 같은 길을 갈 가능성 인정.

---

## 7. 운영 원칙

- **PC 작업과 OCI 작업 분리**: PC 작업은 사용자가 직접 화면 앞에 앉아 진행하는
  발굴/판단 작업이고, OCI 작업은 사용자가 부재중일 때도 자동으로 도는 알림 작업이다.
- **PC 작업 빈도**: 주 2회 예상 (상한 아님). 사용자가 가능한 시점에 PC 앞에서
  발굴/판단 작업을 진행한다. 빈도는 운영하며 조정 가능.
- **OCI 작업 빈도**: 일 3회 자동 PUSH (장초 / 점심 / 장마감 전). OCI 작업은
  알림 배관 유지용이며 메인 의사결정 입력이 아니다.
- **2주 미갱신 알림**: 어떤 산출물이든 2주 이상 갱신되지 않으면 사용자에게 알림.
  운영 단절이 길어졌음을 인지하기 위한 가드.
- **모바일 UI 후순위**: 모바일 UI 는 PC 작업 1~7단계 + OCI 작업 8~11단계가 충분히
  검증된 뒤 12단계에서 다룬다. 현재 단계의 작업 대상 아님.
- **K6/EOD 기준**: 장중 실시간 반응 아님.
- **본업 우선**: 본업 지장 없는 수준에서만 운영.

## 운영 사이클

PC 작업 (주 2회 예상, 상한 아님):

1. universe 갱신 — 한국 상장 ETF universe / 수익률 / TOP N 후보 산출.
2. 후보 해석 — PC 앞에서 직접 후보 목록을 검토.
3. AI 투자세션 토론 — 후보를 AI 와 함께 해석.
4. 매매 결정 또는 보류 — 사용자가 명시적으로 결정.
5. 결정 기록 — 결정 사유 / 보류 사유를 기록 (운영 데이터 누적).
6. 다음 PC 작업 시점 결정.

OCI 작업 (일 3회 자동 PUSH):

7. 보유 종목 상태 브리핑 (보조 PUSH 1).
8. 신규 ETF 관찰 후보 (보조 PUSH 2).
9. 급락 ETF 주의 신호 (보조 PUSH 3).
10. 알림 수신 후 사용자가 PC 작업으로 넘어갈지 판단.

위 항목 중 **메인 의사결정 입력은 PC 작업 1~5단계** 다. OCI 작업은 보조 알림이며
**자동 매수/매도 실행은 어떤 단계에서도 수행하지 않는다**.

---

## 8. 핵심 자산 (Phase 1에서 살려온 것)

**코드 자산** (B 방향 전환 후 처리 분류 — 2026-05-14 / 2026-05-15 갱신):

- ML 모듈 (predictive_risk_classifier, 완전 독립)
  → **보존하되, 현재 B 방향 구조와 fit 검토 후 연결 여부 결정**.
    출력 정의(§3) 가 확정되기 전까지는 자동 연결하지 않는다.
- OCI crontab 구조 (daily_ops, spike_watch, holding_watch)
  → **새 param 구조와 일 3회 자동 PUSH 흐름에 연결 필요**.
    PC 작업 1~5단계가 정착된 뒤 OCI 작업 (운영 사이클 7~10단계) 에 연결.
- Telegram 연동
  → **보조 출력 배관으로 유지**. 메인 의사결정 입력 아님 (§3.2).
- pykrx 가격 조회 배관 (price_history_pykrx.py + universe_refresh.py)
  → **ETF universe 자동 조회 경로는 폐기** (1.0.51 / 1.2.8 / KRX 인증 모두 거부 —
    2026-05-15 가능성 확인 결과). 단일 ticker OHLCV (`get_market_ohlcv`) 만 작동.
    기존 Step6 manual seed 흐름과 holdings 가격 점검 용도로만 보존.
- **FinanceDataReader 배관** (신규, 2026-05-15) — `app/market_data_fdr.py` +
  `app/market_data_store.py` + `app/market_topn.py`
  → B 방향 PC 작업 1~2단계의 **1순위 데이터 소스**. ETF universe (`StockListing("ETF/KR")`,
    1,107개) + ETF 이름 + 카테고리 + 일간/1개월/3개월 수익률 + TOP N 산출.
- **SQLite 시장 데이터 저장소** (신규, 2026-05-15) — `state/market/market_data.sqlite`
  → 테이블 3개 (etf_master / etf_daily_price / market_refresh_log) 만 도입.
    운영 상태 / 승인 / Telegram / Run 상태 / decision evidence 저장은 본 저장소의
    책임이 아니다 — BACKLOG 별도 STEP.
- **KRX OPEN API fallback** — 인증키 승인 대기 (월요일 이후) 후 보조 검증용으로 유지.
  FDR 운영 중 1회라도 실패하면 검증 STEP 진입.

**배움 자산**:
- GPT 주도 설계의 편향 (복잡도 폭발)
- 연구-운영 분리의 함정 (운영이 뒷전 됨)
- 레드팀/설계자 역할 분담 경험
- 단일 파일 누적은 Phase 1 갈아엎기의 직접 원인이었음.
- Phase 2 에서도 POC2-Step5C까지 진행 중 단일 파일 책임 누적 재발 신호가 발견됨.
- 검증자는 기능 산출물은 인정했지만 구조 측면에서는 PARTIALLY_VERIFIED 로 판단함.
- 단일 책임 원칙은 사후 정리가 아니라 매 Step 진입 전 사전 점검 항목이다.
  대응 가드는 KILL_SWITCHES.md 의 KS-10 으로 명문화됨 (2026-04-30, Step5D Cleanup 시점).

---

## 9. 현재 위치 (2026-04-21 기준)

- Phase 1 closeout 완료
- 새 프로젝트 시작 결정 (Phase 2)
- 접근 방식: 화이트리스트 기반 신규 폴더 시작
- Git 태그로 기존 프로젝트 보존, 삭제 아님

결정 (2026-05-14):
1. 현재 배관(Telegram PUSH)은 보조로 보존
2. 방향: B (원래 §2 목표로 회귀)
3. 핵심: 섹터/테마 발굴 + AI 투자세션 시스템 내재화 + ML 연결
4. Step8 운영 검증 중단
5. 다음 작업: 무엇을 살리고 무엇을 새로 만들지 선별

현재 위치 (2026-05-14 추가):
- 현재 프로젝트는 B 방향 전환 직후다.
- 다음 작업은 새 기능 구현이 아니라 PC 작업 1~2단계 가능성 확인이다.
- 시스템 출력 정의는 pykrx 가능성 확인 후 확정한다.
- 확인 범위:
  1. pykrx 로 한국 상장 ETF 전체 universe 조회 가능 여부.
  2. ETF 별 일간 / 1개월 / 3개월 수익률 계산 가능 여부.
  3. 수익률 기준 TOP N 산출 가능 여부.
- TOP N 의 N 값은 고정하지 않고 운영/테스트 중 변경 가능하게 둔다.
- 아직 하지 않는 것:
  - 구성 종목 추출
  - 정적 데이터 화면
  - AI 투자세션 자동화
  - ML 연결
  - OCI 일 3회 자동 PUSH script 연결
  - 모바일 UI

---

## 10. 절대 하지 않을 것

1. 친구 프로젝트를 뼈대로 삼지 않는다 (참조만)
2. MongoDB / 신규 대형 DB / 엔터프라이즈 저장소로 확장하지 않는다.
   - **데이터 종류별 SSOT 분리** (2026-05-18 정정):
     · 시장 시세 / ETF universe / 가격 이력 / TOP N 산출 기준 — **SQLite**
       (`state/market/market_data.sqlite` 단일 파일).
     · holdings / Run / 승인 상태 / Telegram 발송 흐름 — **기존 JSON SSOT 유지**.
   - SQLite 는 시장 데이터 로컬 저장소이며 운영 승인 상태 / Telegram 메시지 /
     decision evidence 저장소가 아니다 (해당 항목은 BACKLOG).
   - 즉 "JSON SSOT 완전 폐기" 가 아니라 **데이터 성격별 SSOT 분리** 다.
3. GPT 설계에 전체 방향을 맡기지 않는다
4. "혹시 필요할까" 로직 아카이브하지 않는다 (Git으로 충분)
5. MDD 10%를 1차 목표로 삼지 않는다